DSA KNOWLEDGE GRAPH SOURCE TEXT
Format: Upgraded educational website format for C++ extraction into JSON.
Total Topics: 87
Note: Image paths are placeholders. Add actual image files into the frontend/images folder using the same names.

Topic ID: 1
Topic: Data
Category: Fundamental Concept
Difficulty: Easy

Definition:
Raw facts and information that can be processed.

Operations:
- None

Relationships:
- None

Time Complexity:
- None

Real Life Examples:
- Student scores
- Temperature readings

Interview Questions:
- What is Data? | Raw facts and information that can be processed.
- When should we use Data? | Use Data when its structure, operations, or performance match the problem requirement, especially in Fundamental Concept topics.

CPP Code:

Python Code:

Java Code:

Images:
- Data Diagram -> images/data_diagram.png
- Data Example -> images/data_example.png

Step By Step:
- Understand the definition of Data
- Identify related concepts
- Study examples and complexity
- Practice with a small problem

==================================================
Topic ID: 2
Topic: Data Structure
Category: Fundamental Concept
Difficulty: Easy

Definition:
A method of organizing and storing data efficiently.

Operations:
- insert
- delete
- search
- traverse

Relationships:
- uses -> Data

Time Complexity:
- None

Real Life Examples:
- Database organization

Interview Questions:
- What is Data Structure? | A method of organizing and storing data efficiently.
- When should we use Data Structure? | Use Data Structure when its structure, operations, or performance match the problem requirement, especially in Fundamental Concept topics.

CPP Code:

Python Code:

Java Code:

Images:
- Data Structure Diagram -> images/data_structure_diagram.png
- Data Structure Example -> images/data_structure_example.png

Step By Step:
- Identify the Data Structure use case
- Choose the required operation such as insert
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 3
Topic: Linear Data Structure
Category: Linear Structure
Difficulty: Easy

Definition:
Data elements arranged sequentially.

Operations:
- insert
- delete
- traverse

Relationships:
- is_a -> Data Structure

Time Complexity:
- None

Real Life Examples:
- Queue line

Interview Questions:
- What is Linear Data Structure? | Data elements arranged sequentially.
- When should we use Linear Data Structure? | Use Linear Data Structure when its structure, operations, or performance match the problem requirement, especially in Linear Structure topics.

CPP Code:

Python Code:

Java Code:

Images:
- Linear Data Structure Diagram -> images/linear_data_structure_diagram.png
- Linear Data Structure Example -> images/linear_data_structure_example.png

Step By Step:
- Identify the Linear Data Structure use case
- Choose the required operation such as insert
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 4
Topic: Array
Category: Linear Structure
Difficulty: Easy

Definition:
A collection of elements stored in contiguous memory locations.

Operations:
- insert
- delete
- access
- traverse

Relationships:
- is_a -> Linear Data Structure
- parent_of -> One-Dimensional Array
- parent_of -> Two-Dimensional Array
- parent_of -> Multidimensional Array

Time Complexity:
- access: O(1)
- search: O(n)
- insert: O(n)
- delete: O(n)

Real Life Examples:
- Student score list

Interview Questions:
- What is Array? | A collection of elements stored in contiguous memory locations.
- When should we use Array? | Use Array when its structure, operations, or performance match the problem requirement, especially in Linear Structure topics.

CPP Code:
int arr[5] = {1,2,3,4,5};

Python Code:

Java Code:

Images:
- Array Diagram -> images/array_diagram.png
- Array Example -> images/array_example.png

Step By Step:
- Identify the Array use case
- Choose the required operation such as insert
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 5
Topic: One-Dimensional Array
Category: Linear Structure
Difficulty: Easy

Definition:
Array containing elements in a single row.

Operations:
- access
- insert
- delete

Relationships:
- None

Time Complexity:
- access: O(1)

Real Life Examples:
- List of exam scores

Interview Questions:
- What is One-Dimensional Array? | Array containing elements in a single row.
- When should we use One-Dimensional Array? | Use One-Dimensional Array when its structure, operations, or performance match the problem requirement, especially in Linear Structure topics.

CPP Code:
int arr[5];

Python Code:

Java Code:

Images:
- One-Dimensional Array Diagram -> images/one_dimensional_array_diagram.png
- One-Dimensional Array Example -> images/one_dimensional_array_example.png

Step By Step:
- Identify the One-Dimensional Array use case
- Choose the required operation such as access
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 6
Topic: Two-Dimensional Array
Category: Linear Structure
Difficulty: Easy

Definition:
Array organized in rows and columns.

Operations:
- access
- insert
- delete

Relationships:
- None

Time Complexity:
- access: O(1)

Real Life Examples:
- Spreadsheet table

Interview Questions:
- What is Two-Dimensional Array? | Array organized in rows and columns.
- When should we use Two-Dimensional Array? | Use Two-Dimensional Array when its structure, operations, or performance match the problem requirement, especially in Linear Structure topics.

CPP Code:
int matrix[3][3];

Python Code:

Java Code:

Images:
- Two-Dimensional Array Diagram -> images/two_dimensional_array_diagram.png
- Two-Dimensional Array Example -> images/two_dimensional_array_example.png

Step By Step:
- Identify the Two-Dimensional Array use case
- Choose the required operation such as access
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 7
Topic: Linked List
Category: Linear Structure
Difficulty: Easy

Definition:
A linked list is a linear data structure made of nodes, where elements are stored non-contiguously in memory. Each node contains data and a pointer to the next node in the sequence.

Operations:
- Insert
- Delete
- Traverse
- Search

Relationships:
- uses -> Node
- uses -> Pointer
- parent_of -> Singly Linked List
- parent_of -> Doubly Linked List
- parent_of -> Circular Linked List

Time Complexity:
- Access: O(n)
- Insert at head: O(1)
- Insert at position: O(n)
- Delete the head: O(1)
- Delete a specific node: O(n) if search is required
- Traverse: O(n)
- Search: O(n)

Real Life Examples:
- Music playlists navigation
- Browser history navigation

Interview Questions:
- What is Linked List? | A linked list is a linear data structure made of nodes, where elements are stored non-contiguously in memory. Each node contains data and a pointer to the next node in the sequence.
- When should we use Linked List? | Use Linked List when its structure, operations, or performance match the problem requirement, especially in Linear Structure topics.

CPP Code:
class Node {
public:
    int data;
    Node *next;

    Node(int value) : data(value), next(nullptr) {}
};

int main() {
    Node* head = new Node(1);
    head->next = new Node(2);
    head->next->next = new Node(3);
    return 0;
}

Python Code:

Java Code:

Images:
- Linked List Diagram -> images/linked_list_diagram.png
- Linked List Example -> images/linked_list_example.png

Step By Step:
- Identify the Linked List use case
- Choose the required operation such as Insert
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 8
Topic: Singly Linked List
Category: Linear Structure
Difficulty: Easy

Definition:
Singly linked list is a linked list where each node points only to the next node in the sequence.

Operations:
- Insert
- Delete
- Traverse
- Search

Relationships:
- child_of -> Linked List
- uses -> Node
- uses -> Pointer
- uses -> Dynamic Memory Allocation

Time Complexity:
- Access: O(n)
- Insert at head: O(1)
- Insert at other position: O(n)
- Delete the head: O(1)
- Delete a specific node: O(n) if search is required
- Traverse: O(n)
- Search: O(n)

Real Life Examples:
- Simple task scheduling
- Browser forward navigation
- Music playlist next song navigation

Interview Questions:
- What is Singly Linked List? | Singly linked list is a linked list where each node points only to the next node in the sequence.
- When should we use Singly Linked List? | Use Singly Linked List when its structure, operations, or performance match the problem requirement, especially in Linear Structure topics.

CPP Code:
class Node {
public:
    int data;
    Node *next;

    Node(int value) : data(value), next(nullptr) {}
};

int main() {
    Node* head = new Node(1);
    head->next = new Node(2);
    head->next->next = nullptr;
    return 0;
}

Python Code:

Java Code:

Images:
- Singly Linked List Diagram -> images/singly_linked_list_diagram.png
- Singly Linked List Example -> images/singly_linked_list_example.png

Step By Step:
- Identify the Singly Linked List use case
- Choose the required operation such as Insert
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 9
Topic: Doubly Linked List
Category: Linear Structure
Difficulty: Easy

Definition:
A linked list where each node contains pointers to both the next and previous nodes.

Operations:
- Insert
- Delete
- Traverse
- Search

Relationships:
- child_of -> Linked List
- uses -> Node
- uses -> Pointer

Time Complexity:
- Access: O(n)
- Insert at head: O(1)
- Insert at tail: O(1) with tail pointer
- Insert at other position: O(n)
- Delete the head: O(1)
- Delete a specific node: O(n) if search is required
- Traverse: O(n)
- Search: O(n)

Real Life Examples:
- Music playlist next and previous song navigation
- Browser back and forward navigation
- Undo and redo functionality in software

Interview Questions:
- What is Doubly Linked List? | A linked list where each node contains pointers to both the next and previous nodes.
- When should we use Doubly Linked List? | Use Doubly Linked List when its structure, operations, or performance match the problem requirement, especially in Linear Structure topics.

CPP Code:
class Node {
public:
    int data;
    Node *next;
    Node *prev;

    Node(int value) : data(value), next(nullptr), prev(nullptr) {}
};

int main() {
    Node* head = new Node(1);
    head->next = new Node(2);
    head->next->prev = head;
    head->next->next = new Node(3);
    head->next->next->prev = head->next;
    return 0;
}

Python Code:

Java Code:

Images:
- Doubly Linked List Diagram -> images/doubly_linked_list_diagram.png
- Doubly Linked List Example -> images/doubly_linked_list_example.png

Step By Step:
- Identify the Doubly Linked List use case
- Choose the required operation such as Insert
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 10
Topic: Circular Linked List
Category: Linear Structure
Difficulty: Easy

Definition:
A linked list where the last node points back to the first node, forming a circular structure.

Operations:
- Insert
- Delete
- Traverse
- Search

Relationships:
- child_of -> Linked List
- uses -> Node
- uses -> Pointer

Time Complexity:
- Access: O(n)
- Insert at head: O(1)
- Insert at tail: O(1) with tail pointer
- Insert at other position: O(n)
- Delete the head: O(1)
- Delete a specific node: O(n) if search is required
- Traverse: O(n)
- Search: O(n)

Real Life Examples:
- Circular buffer implementation
- Round-robin scheduling
- Looping music playlists

Interview Questions:
- What is Circular Linked List? | A linked list where the last node points back to the first node, forming a circular structure.
- When should we use Circular Linked List? | Use Circular Linked List when its structure, operations, or performance match the problem requirement, especially in Linear Structure topics.

CPP Code:
class Node {
public:
    int data;
    Node *next;

    Node(int value) : data(value), next(this) {}
};

int main() {
    Node* head = new Node(1);
    Node* second = new Node(2);
    Node* third = new Node(3);

    head->next = second;
    second->next = third;
    third->next = head;
    return 0;
}

Python Code:

Java Code:

Images:
- Circular Linked List Diagram -> images/circular_linked_list_diagram.png
- Circular Linked List Example -> images/circular_linked_list_example.png

Step By Step:
- Identify the Circular Linked List use case
- Choose the required operation such as Insert
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 11
Topic: Node
Category: Component
Difficulty: Easy

Definition:
A fundamental building block used in data structures such as linked lists, trees, and graphs, where each node stores data and one or more references to other nodes.

Operations:
- Store Data
- Access Data
- Link to Another Node
- Update Reference

Relationships:
- used_in -> Linked List
- used_in -> Tree
- used_in -> Graph
- used_in -> Binary Tree
- used_in -> Binary Search Tree

Time Complexity:
- None

Real Life Examples:
- People connected in a social network
- Webpages connected by hyperlinks

Interview Questions:
- What is Node? | A fundamental building block used in data structures such as linked lists, trees, and graphs, where each node stores data and one or more references to other nodes.
- When should we use Node? | Use Node when its structure, operations, or performance match the problem requirement, especially in Component topics.

CPP Code:
class Node {
public:
    int data;
    Node *next;

    Node(int value) : data(value), next(nullptr) {}
};

Python Code:

Java Code:

Images:
- Node Diagram -> images/node_diagram.png
- Node Example -> images/node_example.png

Step By Step:
- Identify the Node use case
- Choose the required operation such as Store Data
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 12
Topic: Pointer
Category: Memory Concept
Difficulty: Easy

Definition:
A variable that stores the memory address of another variable.

Operations:
- Store Memory Address
- Dereference to Access Value
- Dynamic Memory Allocation
- Reference Memory Location

Relationships:
- used_in -> Node
- used_in -> Linked List
- used_in -> Tree
- used_in -> Graph

Time Complexity:
- None

Real Life Examples:
- Memory management in operating systems
- Navigation systems using memory references
- Dynamic data structures in programming

Interview Questions:
- What is Pointer? | A variable that stores the memory address of another variable.
- When should we use Pointer? | Use Pointer when its structure, operations, or performance match the problem requirement, especially in Memory Concept topics.

CPP Code:
int main() {
    int value = 42;
    int* ptr = &value;
    cout << "Value: " << *ptr << endl;
    return 0;
}

Python Code:

Java Code:

Images:
- Pointer Diagram -> images/pointer_diagram.png
- Pointer Example -> images/pointer_example.png

Step By Step:
- Identify the Pointer use case
- Choose the required operation such as Store Memory Address
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 13
Topic: Dynamic Memory Allocation
Category: Memory Concept
Difficulty: Easy

Definition:
Dynamic memory allocation is the process of allocating memory for a program at runtime (while it is running), rather than at compile time. This allows for more flexible and efficient use of memory, as the program can request and release memory as needed.

Operations:
- Allocate Memory: Using new
- Deallocate Memory: Using delete
- Reallocate Memory
- Access Allocated Data Dynamically

Relationships:
- uses -> Pointer
- used_in -> Linked List
- used_in -> Tree
- used_in -> Graph

Time Complexity:
- Allocate memory: Average O(1)
- Deallocate memory: Average O(1)
- Access allocated memory: O(1)

Real Life Examples:
- Managing memory for applications while they are running
- Creating dynamic data structures like linked lists and trees
- Operating systems allocating memory to processes

Interview Questions:
- What is Dynamic Memory Allocation? | Dynamic memory allocation is the process of allocating memory for a program at runtime (while it is running), rather than at compile time. This allows for more flexible and efficient use of memory, as the program can request and release memory as needed.
- When should we use Dynamic Memory Allocation? | Use Dynamic Memory Allocation when its structure, operations, or performance match the problem requirement, especially in Memory Concept topics.

CPP Code:
#include <iostream>
using namespace std;

int main() {
    int* arr = new int[5]; // Allocate memory for an array of 5 integers
    for (int i = 0; i < 5; i++) {
        arr[i] = i + 1; // Initialize the array
    }
    for (int i = 0; i < 5; i++) {
        cout << arr[i] << " "; // Output the array elements
    }
    delete[] arr; // Deallocate the memory
    return 0;
}

Python Code:

Java Code:

Images:
- Dynamic Memory Allocation Diagram -> images/dynamic_memory_allocation_diagram.png
- Dynamic Memory Allocation Example -> images/dynamic_memory_allocation_example.png

Step By Step:
- Identify the Dynamic Memory Allocation use case
- Choose the required operation such as Allocate Memory
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 14
Topic: Stack
Category: Linear Structure
Difficulty: Easy

Definition:
A linear data structure where operations are performed only at one end, the last element added to the stack is the first to be removed (LIFO: Last In First Out).

Operations:
- push(x): Insert x at the top
- pop(): Remove top element
- Top(): Access top element
- IsEmpty(): Check if stack is empty
- IsFull(): Check if stack is full
- Size(): Check the size of the stack

Relationships:
- uses -> Linked List
- uses -> Array
- uses -> Pointer

Time Complexity:
- Push: O(1)
- Pop: O(1)
- Top: O(1)
- IsEmpty: O(1)
- IsFull: O(1)
- Size: O(1)

Real Life Examples:
- Undo functionality
- Browser back button
- Stack of plates
- Stack of books
- Stack of boxes

Interview Questions:
- What is Stack? | A linear data structure where operations are performed only at one end, the last element added to the stack is the first to be removed (LIFO: Last In First Out).
- When should we use Stack? | Use Stack when its structure, operations, or performance match the problem requirement, especially in Linear Structure topics.

CPP Code:
#include <stack>
using namespace std;

int main() {
    stack<int> s;

    s.push(10);
    s.push(20);
    s.pop();

    return 0;
}

Python Code:

Java Code:

Images:
- Stack Diagram -> images/stack_diagram.png
- Stack Example -> images/stack_example.png

Step By Step:
- Identify the Stack use case
- Choose the required operation such as push(x)
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 15
Topic: Queue
Category: Linear Structure
Difficulty: Easy

Definition:
A linear structure where the first element added is the first to be removed (FIFO: First In First Out), elements are inserted at the rear and removed from the front.

Operations:
- Enqueue(x): Insert x at rear
- Dequeue(): Remove front
- Front(): Access front element
- IsEmpty(): Check if queue is empty
- IsFull(): Check if queue is full
- Size(): Check the size of the queue

Relationships:
- uses -> Linked List
- uses -> Array
- uses -> Node
- parent_of -> Circular Queue
- parent_of -> Priority Queue
- parent_of -> Deque

Time Complexity:
- Enqueue: O(1)
- Dequeue: O(1)
- Front: O(1)
- Size: O(1)
- IsEmpty: O(1)
- IsFull: O(1)

Real Life Examples:
- Store customer lines, first come, first served
- Print queue in a printer
- CPU task scheduling

Interview Questions:
- What is Queue? | A linear structure where the first element added is the first to be removed (FIFO: First In First Out), elements are inserted at the rear and removed from the front.
- When should we use Queue? | Use Queue when its structure, operations, or performance match the problem requirement, especially in Linear Structure topics.

CPP Code:
#include <iostream>
#include <queue>
using namespace std;

int main() {
   queue<int> q;
   q.push(10);
   q.push(20);
   cout << q.front() << endl;
   q.pop();
   return 0;
}

Python Code:

Java Code:

Images:
- Queue Diagram -> images/queue_diagram.png
- Queue Example -> images/queue_example.png

Step By Step:
- Identify the Queue use case
- Choose the required operation such as Enqueue(x)
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 16
Topic: Circular Queue
Category: Linear Structure
Difficulty: Easy

Definition:
A queue where the rear wraps around to the front using circular indexing, efficiently reusing empty spaces created by dequeued elements.

Operations:
- Enqueue(x): Enqueue, insert x at rear
- Dequeue(): Dequeue, remove front
- Front(): Access front element
- IsEmpty(): Check if queue is empty
- IsFull(): Check if queue is full
- Size(): Check the size of the queue

Relationships:
- child_of -> Queue
- uses -> Array
- uses -> Modular Arithmetic

Time Complexity:
- Enqueue: O(1)
- Dequeue: O(1)
- Front: O(1)
- IsEmpty: O(1)
- IsFull: O(1)
- Size: O(1)

Real Life Examples:
- Round-robin scheduling
- Circular buffer implementation
- Traffic lights
- Printer queue

Interview Questions:
- What is Circular Queue? | A queue where the rear wraps around to the front using circular indexing, efficiently reusing empty spaces created by dequeued elements.
- When should we use Circular Queue? | Use Circular Queue when its structure, operations, or performance match the problem requirement, especially in Linear Structure topics.

CPP Code:
#include <iostream>
using namespace std;

int arr[3], front = -1, rear = -1, n = 3;

void enqueue(int x) {
    if ((rear + 1) % n == front) return;
    if (front == -1) front = 0;
    rear = (rear + 1) % n;
    arr[rear] = x;
}

void dequeue() {
    if (front == -1) return;
    if (front == rear) front = rear = -1;
    else front = (front + 1) % n;
}

int main() {
    enqueue(10);
    enqueue(20);
    dequeue();
    enqueue(30);
}

Python Code:

Java Code:

Images:
- Circular Queue Diagram -> images/circular_queue_diagram.png
- Circular Queue Example -> images/circular_queue_example.png

Step By Step:
- Identify the Circular Queue use case
- Choose the required operation such as Enqueue(x)
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 17
Topic: Priority Queue
Category: Specialized Structure
Difficulty: Medium

Definition:
A queue where elements are removed based on priority instead of insertion order.

Operations:
- push(x, p): Inserts new element x with priority p.
- pop(): Remove highest priority element.
- top(): Access the highest priority item

Relationships:
- child_of -> Queue
- uses -> Heap
- used_in -> Dijkstra Algorithm
- used_in -> Prim Algorithm

Time Complexity:
- Push: O(log n)
- Pop: O(log n)
- Top: O(1)

Real Life Examples:
- CPU task scheduling
- Hospital emergency room system
- Network packet routing

Interview Questions:
- What is Priority Queue? | A queue where elements are removed based on priority instead of insertion order.
- When should we use Priority Queue? | Use Priority Queue when its structure, operations, or performance match the problem requirement, especially in Specialized Structure topics.

CPP Code:
#include <iostream>
#include <queue>
using namespace std;

int main() {

   // pair<priority, value>
   priority_queue<pair<int, int>> pq;

   pq.push({3, 100});
   pq.push({5, 200});
   pq.push({1, 300});

   // top()
   cout << "Top Element: "
        << pq.top().second
        << " with priority "
        << pq.top().first << endl;

   pq.pop();

   cout << "After pop(): "
        << pq.top().second << endl;

   return 0;
}

Python Code:

Java Code:

Images:
- Priority Queue Diagram -> images/priority_queue_diagram.png
- Priority Queue Example -> images/priority_queue_example.png

Step By Step:
- Identify the Priority Queue use case
- Choose the required operation such as push(x, p)
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 18
Topic: Deque
Category: Linear Structure
Difficulty: Easy

Definition:
A double-ended queue that allows insertion and deletion at both the front and rear.

Operations:
- push_front(x): Insert an element at the front end.
- push_back(x): Insert an element at the rear end.
- pop_front(): Delete an element from the front end.
- pop_back(): Delete an element from the rear end.
- getFront(): Retrieve (does not remove) the front element.
- getRear(): Retrieve (does not remove) the rear element.
- isEmpty(): Check if the deque is empty.
- size(): Return the number of elements currently in the deque.

Relationships:
- child_of -> Queue
- uses -> Doubly Linked List

Time Complexity:
- Insert: O(1)
- Delete: O(1)
- Get Front or Rear: O(1)
- IsEmpty: O(1)

Real Life Examples:
- Browser history navigation
- Sliding window algorithms

Interview Questions:
- What is Deque? | A double-ended queue that allows insertion and deletion at both the front and rear.
- When should we use Deque? | Use Deque when its structure, operations, or performance match the problem requirement, especially in Linear Structure topics.

CPP Code:
#include <iostream>
#include <deque>
using namespace std;

int main() {
    deque<int> dq;

    dq.push_back(10);
    dq.push_back(20);
    dq.push_front(30);

    // Print deque elements
    for (int x : dq) cout << x << " ";
    cout << endl;

    // Pop from front and back
    dq.pop_front();
    dq.pop_back();

    // Print deque elements after pop
    for (int x : dq) cout << x << " ";

    return 0;
}

Python Code:

Java Code:

Images:
- Deque Diagram -> images/deque_diagram.png
- Deque Example -> images/deque_example.png

Step By Step:
- Identify the Deque use case
- Choose the required operation such as push_front(x)
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 19
Topic: Recursion
Category: Algorithmic Technique
Difficulty: Medium

Definition:
Recursion is a technique where a function solves a problem by calling itself on smaller subproblems.

Operations:
- Recursive Call
- Return

Relationships:
- parent_of -> Recursive Function
- uses -> Call Stack
- used_in -> Tree Traversal
- uses -> Base Case

Time Complexity:
- Depends on: Recursion depth

Real Life Examples:
- Folder traversal
- Tower of hanoi

Interview Questions:
- What is Recursion? | Recursion is a technique where a function solves a problem by calling itself on smaller subproblems.
- When should we use Recursion? | Use Recursion when its structure, operations, or performance match the problem requirement, especially in Algorithmic Technique topics.

CPP Code:

Python Code:

Java Code:

Images:
- Recursion Diagram -> images/recursion_diagram.png
- Recursion Example -> images/recursion_example.png

Step By Step:
- Identify the Recursion use case
- Choose the required operation such as Recursive Call
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 20
Topic: Recursive Function
Category: Algorithm
Difficulty: Medium

Definition:
A function that calls itself to solve smaller instances of a problem.

Operations:
- Self Call

Relationships:
- child_of -> Recursion
- uses -> Call Stack
- used_in -> Tree Traversal
- uses -> Base Case

Time Complexity:
- Depends on: Problem structure and recursion depth

Real Life Examples:
- Factorial calculation
- Tower of hanoi

Interview Questions:
- What is Recursive Function? | A function that calls itself to solve smaller instances of a problem.
- When should we use Recursive Function? | Use Recursive Function when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
#include <iostream>
using namespace std;

int factorial(int n) {
   if (n == 0 || n == 1) return 1;
   return n * factorial(n-1);
}

int main() {
   int n = 5;
   int result = factorial(n);
   cout << result << endl;
   return 0;
}

Python Code:

Java Code:

Images:
- Recursive Function Diagram -> images/recursive_function_diagram.png
- Recursive Function Example -> images/recursive_function_example.png

Step By Step:
- Identify the Recursive Function use case
- Choose the required operation such as Self Call
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 21
Topic: Base Case
Category: Algorithmic Technique
Difficulty: Medium

Definition:
The stopping condition in a recursive algorithm that prevents infinite recursion.

Operations:
- CheckCondition(): Check if the input size is minimal enough to return a value
- ReturnResult(): Return result

Relationships:
- used_in -> Recursion
- used_in -> Recursive Function

Time Complexity:
- CheckCondition(): O(1)
- ReturnResult(): O(1)

Real Life Examples:
- A GPS stopping its search once the destination coordinates are reached
- Walking down a staircase and stopping once our foot touches the ground floor

Interview Questions:
- What is Base Case? | The stopping condition in a recursive algorithm that prevents infinite recursion.
- When should we use Base Case? | Use Base Case when its structure, operations, or performance match the problem requirement, especially in Algorithmic Technique topics.

CPP Code:
int factorial(int n) {
    // The Base Case
    if (n <= 1) {
        return 1;
    }
    // The Recursive Step
    return n * factorial(n - 1);
}

Python Code:

Java Code:

Images:
- Base Case Diagram -> images/base_case_diagram.png
- Base Case Example -> images/base_case_example.png

Step By Step:
- Identify the Base Case use case
- Choose the required operation such as CheckCondition()
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 22
Topic: Call Stack
Category: Memory Concept
Difficulty: Easy

Definition:
A stack used by programs to manage active function calls. Each time a function is called, it is added to the stack, executes its statements, and finally returns to where it was called from.

Operations:
- push (Function Call): The program pushes a new frame into the stack when a function is called.
- pop (Function Return): When a function finishes execution, the frame is popped off.

Relationships:
- is_a -> Stack
- used_in -> Recursion
- used_in -> Recursive Function

Time Complexity:
- Push: O(1)
- Pop: O(1)

Real Life Examples:
- Tracking active function calls in a program
- Managing recursive function execution

Interview Questions:
- What is Call Stack? | A stack used by programs to manage active function calls. Each time a function is called, it is added to the stack, executes its statements, and finally returns to where it was called from.
- When should we use Call Stack? | Use Call Stack when its structure, operations, or performance match the problem requirement, especially in Memory Concept topics.

CPP Code:
#include <iostream>
using namespace std;

void D() {
    float d = 40.5f;
    cout << "In function D\n";
}

void C() {
    double c = 30.5;
    cout << "In function C\n";
}

void B() {
    int b = 20;
    C();
    cout << "In function B\n";
}

void A() {
    int a = 10;
    B();
    cout << "In function A\n";
}

int main() {
    A();
    D();
    return 0;
}

Python Code:

Java Code:

Images:
- Call Stack Diagram -> images/call_stack_diagram.png
- Call Stack Example -> images/call_stack_example.png

Step By Step:
- Identify the Call Stack use case
- Choose the required operation such as push (Function Call)
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 23
Topic: Sparse Matrix
Category: Specialized Structure
Difficulty: Easy

Definition:
A matrix in which most of the elements are zero.

Operations:
- insert(row, column, value): Adds a non-zero element to the matrix
- delete(row, column): Removes a non-zero element.
- traverse(): Iterates through non-zero elements.
- transpose(): Reverse the row and column indices of all non-zero elements.

Relationships:
- child_of -> Matrix
- uses -> Linked List
- used_in -> Graph
- uses -> Array

Time Complexity:
- Insert: O(n)
- Delete: O(n)
- Traverse: O(number of non-zero elements)
- Space: O(number of non-zero elements)

Real Life Examples:
- Adjacency matrix of sparse graphs
- Image compression

Interview Questions:
- What is Sparse Matrix? | A matrix in which most of the elements are zero.
- When should we use Sparse Matrix? | Use Sparse Matrix when its structure, operations, or performance match the problem requirement, especially in Specialized Structure topics.

CPP Code:
struct Node {
    int row, col, val;
    Node* next;
};
// Linked list approach for insertion
void insert(int r, int c, int v) {
    // Implementation logic to link new node
}

Python Code:

Java Code:

Images:
- Sparse Matrix Diagram -> images/sparse_matrix_diagram.png
- Sparse Matrix Example -> images/sparse_matrix_example.png

Step By Step:
- Identify the Sparse Matrix use case
- Choose the required operation such as insert(row, column, value)
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 24
Topic: Polynomial Representation
Category: Representation Technique
Difficulty: Easy

Definition:
A method of storing and manipulating polynomial expressions using arrays or linked lists.

Operations:
- create(): Initialize a new polynomial
- insert(coeff, exp): Add a new term to the polynomial
- display(): Print the polynomial in a + bx^n format
- add(poly1, poly2): Add two polynomials by summing coefficients of like terms
- multiply(poly1, poly2): Multiply every term of poly1 by every term of poly2

Relationships:
- used_in -> Polynomial
- uses -> Array
- uses -> Linked List

Time Complexity:
- Addition: O(n + m) // n, m are number of terms
- Multiplication: O(n*m)
- Evaluation: O(n)

Real Life Examples:
- Equation simplification
- Scientific calculator

Interview Questions:
- What is Polynomial Representation? | A method of storing and manipulating polynomial expressions using arrays or linked lists.
- When should we use Polynomial Representation? | Use Polynomial Representation when its structure, operations, or performance match the problem requirement, especially in Representation Technique topics.

CPP Code:
struct Term {
    int coeff;
    int exp;
    Term* next;
};

void addTerm(Term** poly, int c, int e) {
    Term* newNode = new Term{c, e, nullptr};
    // Implementation to keep terms sorted by exponent
}

Python Code:

Java Code:

Images:
- Polynomial Representation Diagram -> images/polynomial_representation_diagram.png
- Polynomial Representation Example -> images/polynomial_representation_example.png

Step By Step:
- Identify the Polynomial Representation use case
- Choose the required operation such as create()
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 25
Topic: Tree
Category: Non-Linear Structure
Difficulty: Medium

Definition:
A hierarchical data structure consisting of nodes connected by edges with one root node and zero or more subtrees.

Operations:
- insert
- search
- delete
- traverse

Relationships:
- is_a -> Graph

Time Complexity:
- access: O(n)
- search: O(n)
- insert: O(1) to O(n)
- traverse: O(n)

Real Life Examples:
- File systems
- Organizational charts
- Family trees

Interview Questions:
- What is Tree? | A hierarchical data structure consisting of nodes connected by edges with one root node and zero or more subtrees.
- When should we use Tree? | Use Tree when its structure, operations, or performance match the problem requirement, especially in Non-Linear Structure topics.

CPP Code:
struct TreeNode {

int data;

vector<TreeNode*> children;

TreeNode(int val) : data(val) {}

};

Python Code:

Java Code:

Images:
- Tree Diagram -> images/tree_diagram.png
- Tree Example -> images/tree_example.png

Step By Step:
- Identify the Tree use case
- Choose the required operation such as insert
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 26
Topic: Binary Tree
Category: Non-Linear Structure
Difficulty: Medium

Definition:
A tree in which each node has at most two children called the left child and right child.

Operations:
- insert
- delete
- preorder traversal
- inorder traversal
- postorder traversal

Relationships:
- is_a -> Tree

Time Complexity:
- insert: O(n)
- delete: O(n)
- preorder traversal: O(n)
- inorder traversal: O(n)
- postorder traversal: O(n)

Real Life Examples:
- Tournament brackets
- Expression trees
- Decision making/trees

Interview Questions:
- What is Binary Tree? | A tree in which each node has at most two children called the left child and right child.
- When should we use Binary Tree? | Use Binary Tree when its structure, operations, or performance match the problem requirement, especially in Non-Linear Structure topics.

CPP Code:
class Node {

public:

    T data;

    Node* left;

    Node* right;

Node(T value) : data(value), left(nullptr), right(nullptr) {}

};

Python Code:

Java Code:

Images:
- Binary Tree Diagram -> images/binary_tree_diagram.png
- Binary Tree Example -> images/binary_tree_example.png

Step By Step:
- Identify the Binary Tree use case
- Choose the required operation such as insert
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 27
Topic: Binary Search Tree
Category: Non-Linear Structure
Difficulty: Medium

Definition:
A binary tree where all values in the left subtree are smaller than the root and all values in the right subtree are larger than the root.

Operations:
- insert
- delete
- search
- findMin
- findMax

Relationships:
- is_a -> Binary Tree

Time Complexity:
- insert: O(log n) average, O(n) worst
- delete: O(log n) average, O(n) worst
- search: O(log n) average, O(n) worst
- findMin: O(log n) average, O(n) worst
- findMax: O(log n) average, O(n) worst

Real Life Examples:
- Dictionary/ Word lookup
- Databases and Indexing

Interview Questions:
- What is Binary Search Tree? | A binary tree where all values in the left subtree are smaller than the root and all values in the right subtree are larger than the root.
- When should we use Binary Search Tree? | Use Binary Search Tree when its structure, operations, or performance match the problem requirement, especially in Non-Linear Structure topics.

CPP Code:
struct Node {

int data;

Node* left;

Node* right;

Node(int val) : data(val), left(nullptr), right(nullptr) {}

};

Python Code:

Java Code:

Images:
- Binary Search Tree Diagram -> images/binary_search_tree_diagram.png
- Binary Search Tree Example -> images/binary_search_tree_example.png

Step By Step:
- Identify the Binary Search Tree use case
- Choose the required operation such as insert
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 28
Topic: AVL Tree
Category: Non-Linear Structure
Difficulty: Hard

Definition:
A self-balancing binary search tree where the difference between heights of left and right subtrees cannot be more than one for any node.

Operations:
- insert
- delete
- search
- rotateLeft
- rotateRight
- rebalance

Relationships:
- is_a -> Binary Search Tree

Time Complexity:
- insert: O(log n)
- delete: O(log n)
- search: O(log n)
- rotateLeft: O(1)
- rotateRight: O(1)
- rebalance: O(log n)

Real Life Examples:
- Gaming and Simulation Engines
- Memory management in operating systems

Interview Questions:
- What is AVL Tree? | A self-balancing binary search tree where the difference between heights of left and right subtrees cannot be more than one for any node.
- When should we use AVL Tree? | Use AVL Tree when its structure, operations, or performance match the problem requirement, especially in Non-Linear Structure topics.

CPP Code:
struct AVLNode {

public:

int key;

AVLNode* left;

AVLNode* right;

int height;

AVLNode(int val) : key(val), left(nullptr), right(nullptr), height(1) {}

};

Python Code:

Java Code:

Images:
- AVL Tree Diagram -> images/avl_tree_diagram.png
- AVL Tree Example -> images/avl_tree_example.png

Step By Step:
- Identify the AVL Tree use case
- Choose the required operation such as insert
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 29
Topic: Heap
Category: Non-Linear Structure
Difficulty: Medium

Definition:
A complete binary tree that satisfies the heap-order property.

Operations:
- insert
- delete
- heapify
- buildHeap

Relationships:
- is_a -> Complete Binary Tree

Time Complexity:
- insert: O(log n)
- delete: O(log n)
- heapify: O(log n)
- buildHeap: O(n)

Real Life Examples:
- Bandwidth management in networks
- Job scheduling in cloud computing

Interview Questions:
- What is Heap? | A complete binary tree that satisfies the heap-order property.
- When should we use Heap? | Use Heap when its structure, operations, or performance match the problem requirement, especially in Non-Linear Structure topics.

CPP Code:
vector<int> heap = {50, 30, 20, 15, 10};

Python Code:

Java Code:

Images:
- Heap Diagram -> images/heap_diagram.png
- Heap Example -> images/heap_example.png

Step By Step:
- Identify the Heap use case
- Choose the required operation such as insert
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 30
Topic: Max Heap
Category: Non-Linear Structure
Difficulty: Medium

Definition:
A heap where each parent node is greater than or equal to its children.

Operations:
- insert
- deleteMax
- extractMax
- heapify
- buildMaxHeap
- peekMax

Relationships:
- is_a -> Heap
- used_in -> Priority Queue

Time Complexity:
- insert: O(log n)
- deleteMax: O(log n)
- extractMax: O(log n)
- heapify: O(log n)
- buildMaxHeap: O(n)
- peekMax: O(1)

Real Life Examples:
- Gaming leaderboards
- Stock market systems

Interview Questions:
- What is Max Heap? | A heap where each parent node is greater than or equal to its children.
- When should we use Max Heap? | Use Max Heap when its structure, operations, or performance match the problem requirement, especially in Non-Linear Structure topics.

CPP Code:
class MaxHeap {
private:
    vector<T> heap;
    int capacity;
    int size;

public:
    MaxHeap(int cap) : capacity(cap), size(0) {
        heap.resize(capacity);
    }
};

Python Code:

Java Code:

Images:
- Max Heap Diagram -> images/max_heap_diagram.png
- Max Heap Example -> images/max_heap_example.png

Step By Step:
- Identify the Max Heap use case
- Choose the required operation such as insert
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 31
Topic: Min Heap
Category: Non-Linear Structure
Difficulty: Medium

Definition:
A heap where each parent node is less than or equal to its children.

Operations:
- insert
- deleteMin
- extractMin
- heapify
- buildMinHeap
- peekMin

Relationships:
- is_a -> Heap
- used_in -> Dijkstra Algorithm
- used_in -> Priority Queue

Time Complexity:
- insert: O(log n)
- deleteMin: O(log n)
- extractMin: O(log n)
- heapify: O(log n)
- buildMinHeap: O(n)
- peekMin: O(1)

Real Life Examples:
- Navigation systems (shortest path algorithms)
- Task scheduling in operating systems

Interview Questions:
- What is Min Heap? | A heap where each parent node is less than or equal to its children.
- When should we use Min Heap? | Use Min Heap when its structure, operations, or performance match the problem requirement, especially in Non-Linear Structure topics.

CPP Code:
class MinHeap {
private:
    vector<T> heap;
    int capacity;
    int size;

public:
    MinHeap(int cap) : capacity(cap), size(0) {
        heap.resize(capacity);
    }
};

Python Code:

Java Code:

Images:
- Min Heap Diagram -> images/min_heap_diagram.png
- Min Heap Example -> images/min_heap_example.png

Step By Step:
- Identify the Min Heap use case
- Choose the required operation such as insert
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 32
Topic: Adjacency Matrix
Category: Representation Technique
Difficulty: Easy

Definition:
A matrix used to represent a graph, where each cell indicates if an edge exists between two vertices.

Operations:
- storeEdge
- lookupEdge

Relationships:
- used_in -> Graph
- related_to -> Sparse Matrix

Time Complexity:
- lookupEdge: O(1)

Real Life Examples:
- Network connectivity tables

Interview Questions:
- What is Adjacency Matrix? | A matrix used to represent a graph, where each cell indicates if an edge exists between two vertices.
- When should we use Adjacency Matrix? | Use Adjacency Matrix when its structure, operations, or performance match the problem requirement, especially in Representation Technique topics.

CPP Code:
vector<vector<int>> adj(V, vector<int>(V, 0));

Python Code:

Java Code:

Images:
- Adjacency Matrix Diagram -> images/adjacency_matrix_diagram.png
- Adjacency Matrix Example -> images/adjacency_matrix_example.png

Step By Step:
- Identify the Adjacency Matrix use case
- Choose the required operation such as storeEdge
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 33
Topic: Breadth First Search
Category: Algorithm
Difficulty: Medium

Definition:
An algorithm for traversing or searching graph or tree data structures level by level.

Operations:
- enqueue
- dequeue
- visit
- markVisited

Relationships:
- synonym_of -> BFS
- uses -> Queue
- uses -> Graph

Time Complexity:
- traverse: O(V + E)

Real Life Examples:
- Finding shortest paths in unweighted graphs

Interview Questions:
- What is Breadth First Search? | An algorithm for traversing or searching graph or tree data structures level by level.
- When should we use Breadth First Search? | Use Breadth First Search when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
queue<int> q; q.push(src);

Python Code:

Java Code:

Images:
- Breadth First Search Diagram -> images/breadth_first_search_diagram.png
- Breadth First Search Example -> images/breadth_first_search_example.png

Step By Step:
- Identify the Breadth First Search use case
- Choose the required operation such as enqueue
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 34
Topic: Depth First Search
Category: Algorithm
Difficulty: Medium

Definition:
An algorithm for traversing or searching graph or tree data structures by exploring as far down a branch as possible before backtracking.

Operations:
- visit
- recurse
- backtrack

Relationships:
- synonym_of -> DFS
- uses -> Graph
- uses -> Recursion

Time Complexity:
- traverse: O(V + E)

Real Life Examples:
- Maze solving

Interview Questions:
- What is Depth First Search? | An algorithm for traversing or searching graph or tree data structures by exploring as far down a branch as possible before backtracking.
- When should we use Depth First Search? | Use Depth First Search when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
dfs(v) { visited[v]=true; for(n in adj[v]) if(!visited[n]) dfs(n); }

Python Code:

Java Code:

Images:
- Depth First Search Diagram -> images/depth_first_search_diagram.png
- Depth First Search Example -> images/depth_first_search_example.png

Step By Step:
- Identify the Depth First Search use case
- Choose the required operation such as visit
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 35
Topic: Factorial
Category: Mathematics
Difficulty: Easy

Definition:
The product of all positive integers up to a given number, used in recursion and mathematical computations.

Operations:
- compute
- multiply

Relationships:
- used_in -> Base Case
- related_to -> Recursive Function

Time Complexity:
- compute: O(n)

Real Life Examples:
- Counting permutations

Interview Questions:
- What is Factorial? | The product of all positive integers up to a given number, used in recursion and mathematical computations.
- When should we use Factorial? | Use Factorial when its structure, operations, or performance match the problem requirement, especially in Mathematics topics.

CPP Code:
int factorial(int n) { return n <= 1 ? 1 : n * factorial(n - 1); }

Python Code:

Java Code:

Images:
- Factorial Diagram -> images/factorial_diagram.png
- Factorial Example -> images/factorial_example.png

Step By Step:
- Identify the Factorial use case
- Choose the required operation such as compute
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 36
Topic: Fibonacci Sequence
Category: Mathematics
Difficulty: Easy

Definition:
A sequence where each number is the sum of the two preceding numbers, often defined recursively.

Operations:
- generate
- add

Relationships:
- used_in -> Base Case
- related_to -> Recursive Function

Time Complexity:
- generate: O(2^n)

Real Life Examples:
- Population growth models

Interview Questions:
- What is Fibonacci Sequence? | A sequence where each number is the sum of the two preceding numbers, often defined recursively.
- When should we use Fibonacci Sequence? | Use Fibonacci Sequence when its structure, operations, or performance match the problem requirement, especially in Mathematics topics.

CPP Code:
int fib(int n) { return n <= 1 ? n : fib(n - 1) + fib(n - 2); }

Python Code:

Java Code:

Images:
- Fibonacci Sequence Diagram -> images/fibonacci_sequence_diagram.png
- Fibonacci Sequence Example -> images/fibonacci_sequence_example.png

Step By Step:
- Identify the Fibonacci Sequence use case
- Choose the required operation such as generate
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 37
Topic: Level Order Traversal
Category: Algorithm
Difficulty: Medium

Definition:
A tree traversal method that visits nodes level by level, from top to bottom.

Operations:
- enqueue
- dequeue
- visit

Relationships:
- related_to -> Tree Traversal
- uses -> Queue

Time Complexity:
- traverse: O(n)

Real Life Examples:
- Breadth-first exploration

Interview Questions:
- What is Level Order Traversal? | A tree traversal method that visits nodes level by level, from top to bottom.
- When should we use Level Order Traversal? | Use Level Order Traversal when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
queue<Node*> q; q.push(root); while(!q.empty()) { ... }

Python Code:

Java Code:

Images:
- Level Order Traversal Diagram -> images/level_order_traversal_diagram.png
- Level Order Traversal Example -> images/level_order_traversal_example.png

Step By Step:
- Identify the Level Order Traversal use case
- Choose the required operation such as enqueue
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 38
Topic: Memory Management
Category: Computer Science Concept
Difficulty: Easy

Definition:
The process of allocating and freeing memory in a program, often important in dynamic data structures and recursion.

Operations:
- allocate
- deallocate
- manage

Relationships:
- related_to -> Stack
- related_to -> Dynamic Memory Allocation

Time Complexity:
- allocate: O(1)
- deallocate: O(1)

Real Life Examples:
- Managing RAM for running applications

Interview Questions:
- What is Memory Management? | The process of allocating and freeing memory in a program, often important in dynamic data structures and recursion.
- When should we use Memory Management? | Use Memory Management when its structure, operations, or performance match the problem requirement, especially in Computer Science Concept topics.

CPP Code:
int* data = new int[10]; delete[] data;

Python Code:

Java Code:

Images:
- Memory Management Diagram -> images/memory_management_diagram.png
- Memory Management Example -> images/memory_management_example.png

Step By Step:
- Identify the Memory Management use case
- Choose the required operation such as allocate
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 39
Topic: Triplet Representation
Category: Representation Technique
Difficulty: Easy

Definition:
A sparse matrix representation that stores only non-zero elements as triplets of row, column, and value.

Operations:
- storeNonZero
- iterateTriplets

Relationships:
- used_in -> Sparse Matrix

Time Complexity:
- storeNonZero: O(1)

Real Life Examples:
- Storing sparse datasets efficiently

Interview Questions:
- What is Triplet Representation? | A sparse matrix representation that stores only non-zero elements as triplets of row, column, and value.
- When should we use Triplet Representation? | Use Triplet Representation when its structure, operations, or performance match the problem requirement, especially in Representation Technique topics.

CPP Code:
struct Triplet { int row, col, val; };

Python Code:

Java Code:

Images:
- Triplet Representation Diagram -> images/triplet_representation_diagram.png
- Triplet Representation Example -> images/triplet_representation_example.png

Step By Step:
- Identify the Triplet Representation use case
- Choose the required operation such as storeNonZero
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 40
Topic: Graph
Category: Non-Linear Structure
Difficulty: Medium

Definition:
A collection or set of nodes(vertices) connected by edges.

Operations:
- insertVertex
- insertEdge
- removeVertex
- removeEdge
- isEmpty
- getWeight
- getFirstNeighbor
- getNextNeighbor
- DFS
- BFS

Relationships:
- uses -> DFS
- uses -> BFS

Time Complexity:
- insertVertex: O(1)
- insertEdge: O(1)
- removeVertex: O(V + E)
- removeEdge: O(1)
- isEmpty: O(1)
- getWeight: O(1)
- getFirstNeighbor: O(V)
- getNextNeighbor: O(V)
- DFS: O(V + E)
- BFS: O(V + E)

Real Life Examples:
- Airplane flight routes
- Social networks
- Computer networks
- Power grids

Interview Questions:
- What is Graph? | A collection or set of nodes(vertices) connected by edges.
- When should we use Graph? | Use Graph when its structure, operations, or performance match the problem requirement, especially in Non-Linear Structure topics.

CPP Code:
vector<vector<int>> createGraph(int V, vector<vector<int>> &edges) {
    vector<vector<int>> mat(V, vector<int>(V, 0));

    for (auto &it : edges) {
        int u = it[0];
        int v = it[1];
        mat[u][v] = 1;
        mat[v][u] = 1;
    }

    return mat;
}

Python Code:

Java Code:

Images:
- Graph Diagram -> images/graph_diagram.png
- Graph Example -> images/graph_example.png

Step By Step:
- Identify the Graph use case
- Choose the required operation such as insertVertex
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 41
Topic: Directed Graph
Category: Non-Linear Structure
Difficulty: Medium

Definition:
A graph where edges have a direction, indicating a one-way relationship between vertices.

Operations:
- insertVertex
- insertEdge
- removeVertex
- removeEdge
- getInDegree
- getOutDegree
- topologicalSort
- DFS
- BFS

Relationships:
- is_a -> Graph
- used_in -> Topological Sort

Time Complexity:
- insertVertex: O(1)
- insertEdge: O(1)
- removeVertex: O(V + E)
- removeEdge: O(1)
- getInDegree: O(V)
- getOutDegree: O(1)
- topologicalSort: O(V + E)
- DFS: O(V + E)
- BFS: O(V + E)

Real Life Examples:
- Airplane flight routes
- Social media following/follower relationships
- Web page linking

Interview Questions:
- What is Directed Graph? | A graph where edges have a direction, indicating a one-way relationship between vertices.
- When should we use Directed Graph? | Use Directed Graph when its structure, operations, or performance match the problem requirement, especially in Non-Linear Structure topics.

CPP Code:
vector<vector<int>> createGraph(int V, vector<vector<int>> &edges) {
    vector<vector<int>> mat(V, vector<int>(V, 0));

    for (auto &it : edges) {
        int u = it[0];
        int v = it[1];
        mat[u][v] = 1;
    }

    return mat;
}

Python Code:

Java Code:

Images:
- Directed Graph Diagram -> images/directed_graph_diagram.png
- Directed Graph Example -> images/directed_graph_example.png

Step By Step:
- Identify the Directed Graph use case
- Choose the required operation such as insertVertex
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 42
Topic: Undirected Graph
Category: Non-Linear Structure
Difficulty: Medium

Definition:
A graph where edges have no direction, indicating a two-way relationship between vertices.

Operations:
- insertVertex
- insertEdge
- removeVertex
- removeEdge
- getDegree
- findConnectedComponent
- DFS
- BFS

Relationships:
- is_a -> Graph
- used_in -> Minimum Spanning Tree

Time Complexity:
- insertVertex: O(1)
- insertEdge: O(1)
- removeVertex: O(V + E)
- removeEdge: O(1)
- getDegree: O(1)
- findConnectedComponent: O(V + E)
- DFS: O(V + E)
- BFS: O(V + E)

Real Life Examples:
- Road networks
- Social media friend networks(mutual friendships)
- Computer LAN networks

Interview Questions:
- What is Undirected Graph? | A graph where edges have no direction, indicating a two-way relationship between vertices.
- When should we use Undirected Graph? | Use Undirected Graph when its structure, operations, or performance match the problem requirement, especially in Non-Linear Structure topics.

CPP Code:
vector<vector<int>> createGraph(int V, vector<vector<int>> &edges) {
    vector<vector<int>> adj(V);

    for (auto &it : edges) {
        int u = it[0];
        int v = it[1];
        adj[u].push_back(v);
        adj[v].push_back(u);
    }

    return adj;
}

Python Code:

Java Code:

Images:
- Undirected Graph Diagram -> images/undirected_graph_diagram.png
- Undirected Graph Example -> images/undirected_graph_example.png

Step By Step:
- Identify the Undirected Graph use case
- Choose the required operation such as insertVertex
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 43
Topic: Weighted Graph
Category: Non-Linear Structure
Difficulty: Medium

Definition:
A graph where edges have weights, indicating the cost or distance between vertices.

Operations:
- insertVertex
- insertEdge
- removeVertex
- removeEdge
- getWeight
- updateWeight
- shortestPath
- minimumSpanningTree

Relationships:
- is_a -> Graph
- used_in -> Dijkstra Algorithm
- used_in -> Prim Algorithm
- used_in -> Kruskal Algorithm

Time Complexity:
- insertVertex: O(1)
- insertEdge: O(1)
- removeVertex: O(V + E)
- removeEdge: O(1)
- getWeight: O(1)
- updateWeight: O(1)
- shortestPath: O((V + E) log V)
- minimumSpanningTree: O(E log V)

Real Life Examples:
- Google Maps and navigation systems
- Game maps
- Supply chain and logistics networks

Interview Questions:
- What is Weighted Graph? | A graph where edges have weights, indicating the cost or distance between vertices.
- When should we use Weighted Graph? | Use Weighted Graph when its structure, operations, or performance match the problem requirement, especially in Non-Linear Structure topics.

CPP Code:
vector<vector<pair<int, int>>> createWeightedGraph(int V, vector<vector<int>> &edges) {
    vector<vector<pair<int, int>>> adj(V);

    for (auto &it : edges) {
        int u = it[0];
        int v = it[1];
        int weight = it[2];

        adj[u].push_back({v, weight});
        adj[v].push_back({u, weight});
    }

    return adj;
}

Python Code:

Java Code:

Images:
- Weighted Graph Diagram -> images/weighted_graph_diagram.png
- Weighted Graph Example -> images/weighted_graph_example.png

Step By Step:
- Identify the Weighted Graph use case
- Choose the required operation such as insertVertex
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 44
Topic: DFS (Depth First Search) Algorithm
Category: Algorithm
Difficulty: Medium

Definition:
An algorithm for traversing or searching tree or graph data structures, starting at the root and exploring as far as possible along each branch before backtracking.

Operations:
- visitVertex
- markVisited
- getFirstNeighbor
- getNextNeighbor
- backtrack
- traverse

Relationships:
- related_to -> Graph
- related_to -> Tree
- used_in -> Connected Components
- used_in -> Spanning Tree
- used_in -> Topological Sort

Time Complexity:
- visitVertex: O(1)
- markVisited: O(1)
- getFirstNeighbor: O(deg(V))
- getNextNeighbor: O(deg(V))
- backtrack: O(1)
- traverse: O(V + E)

Real Life Examples:
- File system navigation
- Maze solving

Interview Questions:
- What is DFS (Depth First Search) Algorithm? | An algorithm for traversing or searching tree or graph data structures, starting at the root and exploring as far as possible along each branch before backtracking.
- When should we use DFS (Depth First Search) Algorithm? | Use DFS (Depth First Search) Algorithm when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
void dfsRec(vector<vector<int>> &adj, vector<bool> &visited, int s, vector<int> &res) {
    visited[s] = true;
    res.push_back(s);

    for (int i : adj[s]) {
        if (!visited[i]) {
            dfsRec(adj, visited, i, res);
        }
    }

vector<int> dfs(vector<vector<int>> &adj) {
    vector<bool> visited(adj.size(), false);
    vector<int> res;
    dfsRec(adj, visited, 0, res);
    return res;
}

Python Code:

Java Code:

Images:
- DFS (Depth First Search) Algorithm Diagram -> images/dfs_depth_first_search_algorithm_diagram.png
- DFS (Depth First Search) Algorithm Example -> images/dfs_depth_first_search_algorithm_example.png

Step By Step:
- Identify the DFS (Depth First Search) Algorithm use case
- Choose the required operation such as visitVertex
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 45
Topic: BFS (Breadth First Search) Algorithm
Category: Algorithm
Difficulty: Medium

Definition:
An algorithm for traversing or searching tree or graph data structures, starting at the root and exploring all neighbors at the present depth prior to moving to the next level(exploring nodes level-by-level).

Operations:
- visitVertex
- markVisited
- enqueue
- dequeue
- getFirstNeighbor
- getNextNeighbor
- traverse

Relationships:
- related_to -> Graph
- related_to -> Tree
- used_in -> Shortest Path (unweighted graphs)

Time Complexity:
- visitVertex: O(1)
- markVisited: O(1)
- enqueue: O(1)
- dequeue: O(1)
- getFirstNeighbor: O(deg(V))
- getNextNeighbor: O(deg(V))
- traverse: O(V + E)

Real Life Examples:
- Connected Component Detection
- Web Crawlers

Interview Questions:
- What is BFS (Breadth First Search) Algorithm? | An algorithm for traversing or searching tree or graph data structures, starting at the root and exploring all neighbors at the present depth prior to moving to the next level(exploring nodes level-by-level).
- When should we use BFS (Breadth First Search) Algorithm? | Use BFS (Breadth First Search) Algorithm when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
vector<int> bfs(vector<vector<int>>& adj) {
    int V = adj.size();
    vector<bool> visited(V, false);
    vector<int> res;

    queue<int> q;

    int src = 0;
    visited[src] = true;
    q.push(src);

    while (!q.empty()) {
        int curr = q.front();
        q.pop();
        res.push_back(curr);

        for (int x : adj[curr]) {
            if (!visited[x]) {
                visited[x] = true;
                q.push(x);
            }
        }
    }

    return res;
}

Python Code:

Java Code:

Images:
- BFS (Breadth First Search) Algorithm Diagram -> images/bfs_breadth_first_search_algorithm_diagram.png
- BFS (Breadth First Search) Algorithm Example -> images/bfs_breadth_first_search_algorithm_example.png

Step By Step:
- Identify the BFS (Breadth First Search) Algorithm use case
- Choose the required operation such as visitVertex
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 46
Topic: Dijkstra Algorithm
Category: Algorithm
Difficulty: Hard

Definition:
A greedy shortest-path algorithm for weighted graphs with non-negative weights.

Operations:
- initializeDistance
- chooseMinimumDistanceVertex
- relaxEdge
- updateDistance
- updatePath
- findShortestPath

Relationships:
- uses -> Weighted Graph
- uses -> Priority Queue
- uses -> Min Heap

Time Complexity:
- initializeDistance: O(V)
- chooseMinimumDistanceVertex: O(V)
- relaxEdge: O(1)
- updateDistance: O(1)
- updatePath: O(1)
- findShortestPath: O((V + E) log V)

Real Life Examples:
- Robot Navigation
- Network cable routing

Interview Questions:
- What is Dijkstra Algorithm? | A greedy shortest-path algorithm for weighted graphs with non-negative weights.
- When should we use Dijkstra Algorithm? | Use Dijkstra Algorithm when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
vector<int> dijkstra(vector<vector<pair<int,int>>>& adj, int src) {

    int V = adj.size();

    priority_queue<pair<int, int>, vector<pair<int, int>>, greater<pair<int, int>>> pq;

    vector<int> dist(V, INT_MAX);

    dist[src] = 0;
    pq.emplace(0, src);

    while (!pq.empty()) {
        auto top = pq.top();
        pq.pop();

        int d = top.first;
        int u = top.second;

        if (d > dist[u])
            continue;

        for (auto &p : adj[u]) {
            int v = p.first;
            int w = p.second;

            if (dist[u] + w < dist[v]) {
                dist[v] = dist[u] + w;
                pq.emplace(dist[v], v);
            }
        }
    }

    return dist;
}

Python Code:

Java Code:

Images:
- Dijkstra Algorithm Diagram -> images/dijkstra_algorithm_diagram.png
- Dijkstra Algorithm Example -> images/dijkstra_algorithm_example.png

Step By Step:
- Identify the Dijkstra Algorithm use case
- Choose the required operation such as initializeDistance
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 47
Topic: Minimum Spanning Tree
Category: Algorithm
Difficulty: Hard

Definition:
A spanning tree connecting all vertices with minimum total edge weight and no cycles.

Operations:
- selectMinimumEdge
- addEdge
- connectVertices
- avoidCycle
- calculateTotalCost

Relationships:
- is_a -> Tree
- depends_on -> Weighted Undirected Graph
- implements -> Prim Algorithm
- implements -> Kruskal Algorithm

Time Complexity:
- selectMinimumEdge: O(E log E)
- addEdge: O(1)
- connectVertices: O(1)
- avoidCycle: O(alpha(V))
- calculateTotalCost: O(V)

Real Life Examples:
- Water pipeline Systems
- Railway System Planning

Interview Questions:
- What is Minimum Spanning Tree? | A spanning tree connecting all vertices with minimum total edge weight and no cycles.
- When should we use Minimum Spanning Tree? | Use Minimum Spanning Tree when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
vector<pair<int, pair<int, int>>> minimumSpanningTree(int V, vector<vector<int>>& edges) {
    sort(edges.begin(), edges.end(), [](auto &a, auto &b) {
        return a[2] < b[2];
    });

    vector<pair<int, pair<int, int>>> mst;

    for (auto &e : edges) {
        int u = e[0];
        int v = e[1];
        int w = e[2];

        mst.push_back({w, {u, v}});
    }

    return mst;
}

Python Code:

Java Code:

Images:
- Minimum Spanning Tree Diagram -> images/minimum_spanning_tree_diagram.png
- Minimum Spanning Tree Example -> images/minimum_spanning_tree_example.png

Step By Step:
- Identify the Minimum Spanning Tree use case
- Choose the required operation such as selectMinimumEdge
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 48
Topic: Prim Algorithm
Category: Algorithm
Difficulty: Hard

Definition:
A greedy MST algorithm expanding the tree using the minimum outgoing edge.

Operations:
- initializeLowcost
- initializeNearvex
- selectMinimumEdge
- addVertex
- updateLowcost
- buildMST

Relationships:
- parent_of -> Minimum Spanning Tree
- uses -> Weighted Graph

Time Complexity:
- initializeLowcost: O(V)
- initializeNearvex: O(V)
- selectMinimumEdge: O(V)
- addVertex: O(1)
- updateLowcost: O(V)
- buildMST: O(V^2)

Real Life Examples:
- Sensor Networks
- Computer Chip Design

Interview Questions:
- What is Prim Algorithm? | A greedy MST algorithm expanding the tree using the minimum outgoing edge.
- When should we use Prim Algorithm? | Use Prim Algorithm when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
vector<pair<int, int>> prim(int V, vector<vector<pair<int, int>>> &adj) {
    priority_queue<pair<int, int>, vector<pair<int, int>>, greater<pair<int, int>>> pq;
    vector<int> key(V, INT_MAX);
    vector<int> parent(V, -1);
    vector<bool> inMST(V, false);

    pq.push({0, 0});
    key[0] = 0;

    while (!pq.empty()) {
        int u = pq.top().second;
        pq.pop();

        if (inMST[u])
            continue;

        inMST[u] = true;

        for (auto &x : adj[u]) {
            int v = x.first;
            int weight = x.second;

            if (!inMST[v] && weight < key[v]) {
                key[v] = weight;
                pq.push({key[v], v});
                parent[v] = u;
            }
          }
    }

    vector<pair<int, int>> mst;
    for (int i = 1; i < V; i++)
        mst.push_back({parent[i], i});

    return mst;
}

Python Code:

Java Code:

Images:
- Prim Algorithm Diagram -> images/prim_algorithm_diagram.png
- Prim Algorithm Example -> images/prim_algorithm_example.png

Step By Step:
- Identify the Prim Algorithm use case
- Choose the required operation such as initializeLowcost
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 49
Topic: Kruskal Algorithm
Category: Algorithm
Difficulty: Hard

Definition:
A greedy MST algorithm sorting edges by weight and adding them if they don't form a cycle/ A greedy MST algorithm selecting the smallest non-cycling edge.

Operations:
- sortEdges
- selectMinimumEdge
- checkCycle
- mergeComponents
- buildMST

Relationships:
- parent_of -> Minimum Spanning Tree
- uses -> Weighted Graph

Time Complexity:
- sortEdges: O(E log E)
- selectMinimumEdge: O(1)
- checkCycle: O(alpha(V))
- mergeComponents: O(alpha(V))
- buildMST: O(E log E)

Real Life Examples:
- Circuit Wiring
- Cluster Analysis

Interview Questions:
- What is Kruskal Algorithm? | A greedy MST algorithm sorting edges by weight and adding them if they don't form a cycle/ A greedy MST algorithm selecting the smallest non-cycling edge.
- When should we use Kruskal Algorithm? | Use Kruskal Algorithm when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
vector<vector<int>> kruskal(int V, vector<vector<int>>& edges) {
    sort(edges.begin(), edges.end(), [](auto &a, auto &b) {
        return a[2] < b[2];
    });

    vector<vector<int>> mst;

    for (auto &e : edges) {
        int u = e[0];
        int v = e[1];
        int w = e[2];

        mst.push_back({u, v, w});
    }

    return mst;
}

Python Code:

Java Code:

Images:
- Kruskal Algorithm Diagram -> images/kruskal_algorithm_diagram.png
- Kruskal Algorithm Example -> images/kruskal_algorithm_example.png

Step By Step:
- Identify the Kruskal Algorithm use case
- Choose the required operation such as sortEdges
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 50
Topic: Topological Sorting
Category: Algorithm
Difficulty: Hard

Definition:
A linear ordering of vertices in a Directed Acyclic Graph (DAG).

Operations:
- computeInDegree
- selectZeroInDegreeVertex
- outputVertex
- deleteVertex
- deleteOutgoingEdges
- detectCycle

Relationships:
- uses -> DAG
- uses -> Directed Graph
- implements -> DFS
- implements -> BFS

Time Complexity:
- computeInDegree: O(V + E)
- selectZeroInDegreeVertex: O(1)
- outputVertex: O(1)
- deleteVertex: O(1)
- deleteOutgoingEdges: O(outDegree(v))
- detectCycle: O(V)

Real Life Examples:
- Package Management
- Project Task Scheduling

Interview Questions:
- What is Topological Sorting? | A linear ordering of vertices in a Directed Acyclic Graph (DAG).
- When should we use Topological Sorting? | Use Topological Sorting when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
vector<int> topologicalSort(int V, vector<vector<int>>& adj) {
    vector<int> indegree(V, 0);

    for (int u = 0; u < V; u++)
        for (int v : adj[u])
            indegree[v]++;

    queue<int> q;

    for (int i = 0; i < V; i++)
        if (indegree[i] == 0)
            q.push(i);

    vector<int> topo;

    while (!q.empty()) {
        int u = q.front();
        q.pop();

        topo.push_back(u);

        for (int v : adj[u]) {
            indegree[v]--;

            if (indegree[v] == 0)
                q.push(v);
        }
    }

    return topo;
}

Python Code:

Java Code:

Images:
- Topological Sorting Diagram -> images/topological_sorting_diagram.png
- Topological Sorting Example -> images/topological_sorting_example.png

Step By Step:
- Identify the Topological Sorting use case
- Choose the required operation such as computeInDegree
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 51
Topic: Hash Table
Category: Specialized Structure
Difficulty: Medium

Definition:
A data structure that maps keys to values using a hash function(uses hash indexing).

Operations:
- insert
- delete
- search
- hash
- resolveCollision
- rehash

Relationships:
- uses -> Hash Function
- depends_on -> Collision Resolution

Time Complexity:
- insert: O(1) to O(n)
- delete: O(1) to O(n)
- search: O(1) to O(n)
- hash: O(1)
- resolveCollision: O(1) to O(n)
- rehash: O(n)

Real Life Examples:
- Blockchain Systems
- Password Authentication

Interview Questions:
- What is Hash Table? | A data structure that maps keys to values using a hash function(uses hash indexing).
- When should we use Hash Table? | Use Hash Table when its structure, operations, or performance match the problem requirement, especially in Specialized Structure topics.

CPP Code:
class HashTable {
private:
    vector<list<pair<int, string>>> table;
    int size;

    int hashFunction(int key) {
        return key % size;
    }

public:
    HashTable(int s) : size(s) {
        table.resize(size);
    }
};

Python Code:

Java Code:

Images:
- Hash Table Diagram -> images/hash_table_diagram.png
- Hash Table Example -> images/hash_table_example.png

Step By Step:
- Identify the Hash Table use case
- Choose the required operation such as insert
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 52
Topic: Hash Function
Category: Component
Difficulty: Easy

Definition:
A function mapping keys to table indices.

Operations:
- computeHashCode
- mapKeyToIndex
- compressIndex

Relationships:
- used_in -> Hash Table

Time Complexity:
- computeHashCode: O(1)
- mapKeyToIndex: O(1)
- compressIndex: O(1)

Real Life Examples:
- Digital Signatures
- Distributed Systems

Interview Questions:
- What is Hash Function? | A function mapping keys to table indices.
- When should we use Hash Function? | Use Hash Function when its structure, operations, or performance match the problem requirement, especially in Component topics.

CPP Code:
class HashFunction {
public:
    int computeHashCode(int key) {
        return key;
    }

    int mapKeyToIndex(int key, int tableSize) {
        return computeHashCode(key) % tableSize;
    }
};

Python Code:

Java Code:

Images:
- Hash Function Diagram -> images/hash_function_diagram.png
- Hash Function Example -> images/hash_function_example.png

Step By Step:
- Identify the Hash Function use case
- Choose the required operation such as computeHashCode
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 53
Topic: Collision Resolution
Category: Component
Difficulty: Easy

Definition:
Methods for handling multiple keys mapped to the same index.

Operations:
- linearProbing
- quadraticProbing
- doubleHashing
- chaining
- rehashing

Relationships:
- used_in -> Hash Table

Time Complexity:
- linearProbing: O(1) to O(n)
- quadraticProbing: O(1) to O(n)
- doubleHashing: O(1) to O(n)
- chaining: O(1) to O(n)
- rehashing: O(n)

Real Life Examples:
- Database Systems
- Web Browser Caching

Interview Questions:
- What is Collision Resolution? | Methods for handling multiple keys mapped to the same index.
- When should we use Collision Resolution? | Use Collision Resolution when its structure, operations, or performance match the problem requirement, especially in Component topics.

CPP Code:
class HashTable {
private:
    vector<int> table;
    int size;

public:
    HashTable(int s) : size(s) {
        table.resize(size, -1);
    }

    void linearProbing(int key) {
        int index = key % size;

        while (table[index] != -1) {
            index = (index + 1) % size;
        }

        table[index] = key;
    }
};

Python Code:

Java Code:

Images:
- Collision Resolution Diagram -> images/collision_resolution_diagram.png
- Collision Resolution Example -> images/collision_resolution_example.png

Step By Step:
- Identify the Collision Resolution use case
- Choose the required operation such as linearProbing
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 54
Topic: Sorting Algorithm
Category: Algorithm
Difficulty: Medium

Definition:
Arranging elements into a specific order.

Operations:
- compare
- swap
- reorder
- partition
- merge

Relationships:
- parent_of -> Bubble Sort
- parent_of -> Selection Sort
- parent_of -> Insertion Sort
- parent_of -> Merge Sort
- parent_of -> Quick Sort

Time Complexity:
- compare: O(1)
- swap: O(1)
- reorder: O(n)
- partition: O(n)
- merge: O(n)

Real Life Examples:
- Student ranking system
- Search engine result ranking

Interview Questions:
- What is Sorting Algorithm? | Arranging elements into a specific order.
- When should we use Sorting Algorithm? | Use Sorting Algorithm when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
void sortArray(vector<int>& arr) {
    sort(arr.begin(), arr.end());
}

Python Code:

Java Code:

Images:
- Sorting Algorithm Diagram -> images/sorting_algorithm_diagram.png
- Sorting Algorithm Example -> images/sorting_algorithm_example.png

Step By Step:
- Identify the Sorting Algorithm use case
- Choose the required operation such as compare
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 55
Topic: Bubble Sort
Category: Algorithm
Difficulty: Medium

Definition:
A sorting algorithm that repeatedly steps through the list, compares adjacent elements and swaps them if they are in the wrong order.

Operations:
- compareAdjacent
- swapAdjacent
- repeatPass

Relationships:
- is_a -> Sorting Algorithm

Time Complexity:
- compareAdjacent: O(1)
- swapAdjacent: O(1)
- repeatPass: O(n)

Real Life Examples:
- Alphabetical Word Sorting
- Game Leaderboards

Interview Questions:
- What is Bubble Sort? | A sorting algorithm that repeatedly steps through the list, compares adjacent elements and swaps them if they are in the wrong order.
- When should we use Bubble Sort? | Use Bubble Sort when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
void bubbleSort(vector<int>& arr) {
    int n = arr.size();
    bool swapped;

    for (int i = 0; i < n - 1; i++) {
        swapped = false;

        for (int j = 0; j < n - i - 1; j++) {
            if (arr[j] > arr[j + 1]) {
                swap(arr[j], arr[j + 1]);
                swapped = true;
            }
        }

        if (!swapped)
            break;
    }
}

Python Code:

Java Code:

Images:
- Bubble Sort Diagram -> images/bubble_sort_diagram.png
- Bubble Sort Example -> images/bubble_sort_example.png

Step By Step:
- Identify the Bubble Sort use case
- Choose the required operation such as compareAdjacent
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 56
Topic: Selection Sort
Category: Algorithm
Difficulty: Medium

Definition:
A sorting algorithm repeatedly selecting the minimum element.

Operations:
- findMinimum
- swap
- selectNextPosition

Relationships:
- is_a -> Sorting Algorithm

Time Complexity:
- findMinimum: O(n)
- swap: O(1)
- selectNextPosition: O(1)

Real Life Examples:
- Sorting playing cards by rank
- Organizing books alphabetically by title

Interview Questions:
- What is Selection Sort? | A sorting algorithm repeatedly selecting the minimum element.
- When should we use Selection Sort? | Use Selection Sort when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
void selectionSort(vector<int>& arr) {
    int n = arr.size();

    for (int i = 0; i < n - 1; i++) {
        int minIndex = i;

        for (int j = i + 1; j < n; j++) {
            if (arr[j] < arr[minIndex]) {
                minIndex = j;
            }
        }

        swap(arr[i], arr[minIndex]);
    }
}

Python Code:

Java Code:

Images:
- Selection Sort Diagram -> images/selection_sort_diagram.png
- Selection Sort Example -> images/selection_sort_example.png

Step By Step:
- Identify the Selection Sort use case
- Choose the required operation such as findMinimum
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 57
Topic: Insertion Sort
Category: Algorithm
Difficulty: Medium

Definition:
A sorting algorithm building the sorted array one item at a time by repeatedly taking the next item and inserting it into the correct position.

Operations:
- compare
- shift
- insertIntoSortedPart

Relationships:
- is_a -> Sorting Algorithm

Time Complexity:
- compare: O(n)
- shift: O(n)
- insertIntoSortedPart: O(n)

Real Life Examples:
- Sorting playing cards in hand
- Organizing files by date modified

Interview Questions:
- What is Insertion Sort? | A sorting algorithm building the sorted array one item at a time by repeatedly taking the next item and inserting it into the correct position.
- When should we use Insertion Sort? | Use Insertion Sort when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
void insertionSort(vector<int>& arr) {
    int n = arr.size();

    for (int i = 1; i < n; i++) {
        int key = arr[i];
        int j = i - 1;

        while (j >= 0 && arr[j] > key) {
            arr[j + 1] = arr[j];
            j--;
        }

        arr[j + 1] = key;
    }
}

Python Code:

Java Code:

Images:
- Insertion Sort Diagram -> images/insertion_sort_diagram.png
- Insertion Sort Example -> images/insertion_sort_example.png

Step By Step:
- Identify the Insertion Sort use case
- Choose the required operation such as compare
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 58
Topic: Merge Sort
Category: Algorithm
Difficulty: Medium

Definition:
A sorting algorithm that divides the array into smaller subarrays, sorts them, and then merges them back together.

Operations:
- divide
- merge
- recursiveSort

Relationships:
- is_a -> Sorting Algorithm
- uses -> Recursion

Time Complexity:
- divide: O(1)
- merge: O(n)
- recursiveSort: O(n log n)

Real Life Examples:
- Sorting large database records
- External sorting of files on disk

Interview Questions:
- What is Merge Sort? | A sorting algorithm that divides the array into smaller subarrays, sorts them, and then merges them back together.
- When should we use Merge Sort? | Use Merge Sort when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
void merge(vector<int>& arr, int left, int mid, int right);

void mergeSort(vector<int>& arr, int left, int right) {
    if (left >= right) return;

    int mid = left + (right - left) / 2;
    mergeSort(arr, left, mid);
    mergeSort(arr, mid + 1, right);
    merge(arr, left, mid, right);
}

void merge(vector<int>& arr, int left, int mid, int right) {
    vector<int> temp(right - left + 1);
    int i = left, j = mid + 1, k = 0;

    while (i <= mid && j <= right) {
        if (arr[i] <= arr[j]) {
            temp[k++] = arr[i++];
        } else {
            temp[k++] = arr[j++];
        }
    }

    while (i <= mid) {
        temp[k++] = arr[i++];
    }

    while (j <= right) {
        temp[k++] = arr[j++];
    }

    for (int i = 0; i < k; i++) {
        arr[left + i] = temp[i];
    }
}

Python Code:

Java Code:

Images:
- Merge Sort Diagram -> images/merge_sort_diagram.png
- Merge Sort Example -> images/merge_sort_example.png

Step By Step:
- Identify the Merge Sort use case
- Choose the required operation such as divide
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 59
Topic: Quick Sort
Category: Algorithm
Difficulty: Medium

Definition:
A sorting algorithm that picks an element as a pivot and partitions the array around the pivot(A divide-and-conquer sorting algorithm partitioning around a pivot).

Operations:
- choosePivot
- partition
- swap
- recursiveSort

Relationships:
- is_a -> Sorting Algorithm
- uses -> Recursion

Time Complexity:
- choosePivot: O(1)
- partition: O(n)
- swap: O(1)
- recursiveSort: O(n log n)

Real Life Examples:
- E-commerce product sorting
- File system organization

Interview Questions:
- What is Quick Sort? | A sorting algorithm that picks an element as a pivot and partitions the array around the pivot(A divide-and-conquer sorting algorithm partitioning around a pivot).
- When should we use Quick Sort? | Use Quick Sort when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
int partition(vector<int>& arr, int low, int high) {
    int pivot = arr[high];
    int i = low - 1;

    for (int j = low; j < high; j++) {
        if (arr[j] < pivot) {
            i++;
            swap(arr[i], arr[j]);
        }
    }

    swap(arr[i + 1], arr[high]);
    return i + 1;
}

void quickSort(vector<int>& arr, int low, int high) {
    if (low < high) {
        int pi = partition(arr, low, high);

        quickSort(arr, low, pi - 1);
        quickSort(arr, pi + 1, high);
    }
}

Python Code:

Java Code:

Images:
- Quick Sort Diagram -> images/quick_sort_diagram.png
- Quick Sort Example -> images/quick_sort_example.png

Step By Step:
- Identify the Quick Sort use case
- Choose the required operation such as choosePivot
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 60
Topic: Binary Search
Category: Algorithm
Difficulty: Medium

Definition:
A searching algorithm repeatedly dividing a sorted search space in half.

Operations:
- findMiddle
- compareTarget
- updateLeftBoundary
- updateRightBoundary
- returnIndex

Relationships:
- depends_on -> Sorted Data
- related_to -> Binary Search Tree

Time Complexity:
- findMiddle: O(1)
- compareTarget: O(1)
- updateLeftBoundary: O(1)
- updateRightBoundary: O(1)
- returnIndex: O(1)

Real Life Examples:
- Database query optimization
- File system search

Interview Questions:
- What is Binary Search? | A searching algorithm repeatedly dividing a sorted search space in half.
- When should we use Binary Search? | Use Binary Search when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
int binarySearch(vector<int>& arr, int target, int low, int high) {
    if (low > high) return -1;

    int mid = low + (high - low) / 2;
    if (arr[mid] == target) return mid;

    if (arr[mid] > target) return binarySearch(arr, target, low, mid - 1);
    return binarySearch(arr, target, mid + 1, high);
}

Python Code:

Java Code:

Images:
- Binary Search Diagram -> images/binary_search_diagram.png
- Binary Search Example -> images/binary_search_example.png

Step By Step:
- Identify the Binary Search use case
- Choose the required operation such as findMiddle
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 61
Topic: STL (Standard Template Library) Components
Category: Specialized Structure
Difficulty: Easy

Definition:
Standard Template Library components in C++ including containers, iterators, algorithms, and functors.

Operations:
- storeElements
- insert
- delete
- search
- sort
- iterate
- applyAlgorithm

Relationships:
- parent_of -> Vector
- parent_of -> Queue
- parent_of -> Stack
- parent_of -> Map
- parent_of -> Set
- parent_of -> Priority Queue

Time Complexity:
- storeElements: O(n)
- insert: O(1) to O(n)
- delete: O(1) to O(n)
- search: O(1) to O(n)
- sort: O(n log n)
- iterate: O(n)
- applyAlgorithm: O(n)

Real Life Examples:
- Database record management using vectors and maps
- Browser history management using stacks

Interview Questions:
- What is STL (Standard Template Library) Components? | Standard Template Library components in C++ including containers, iterators, algorithms, and functors.
- When should we use STL (Standard Template Library) Components? | Use STL (Standard Template Library) Components when its structure, operations, or performance match the problem requirement, especially in Specialized Structure topics.

CPP Code:
#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    vector<int> nums = {5, 2, 8, 1, 3};

    sort(nums.begin(), nums.end());

    for (int x : nums) {
        cout << x << " ";
    }

    return 0;
}

Python Code:

Java Code:

Images:
- STL (Standard Template Library) Components Diagram -> images/stl_standard_template_library_components_diagram.png
- STL (Standard Template Library) Components Example -> images/stl_standard_template_library_components_example.png

Step By Step:
- Identify the STL (Standard Template Library) Components use case
- Choose the required operation such as storeElements
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 62
Topic: Multidimensional Array
Category: Linear Structure
Difficulty: Easy

Definition:
An array with more than two dimensions.

Operations:
- access
- insert
- delete
- traverse

Relationships:
- child_of -> Array

Time Complexity:
- access: O(1)
- search: O(n)

Real Life Examples:
- 3D game coordinates
- Scientific simulation data

Interview Questions:
- What is Multidimensional Array? | An array with more than two dimensions.
- When should we use Multidimensional Array? | Use Multidimensional Array when its structure, operations, or performance match the problem requirement, especially in Linear Structure topics.

CPP Code:
int arr[2][3][4];

Python Code:

Java Code:

Images:
- Multidimensional Array Diagram -> images/multidimensional_array_diagram.png
- Multidimensional Array Example -> images/multidimensional_array_example.png

Step By Step:
- Identify the Multidimensional Array use case
- Choose the required operation such as access
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 63
Topic: Modular Arithmetic
Category: Mathematics
Difficulty: Easy

Definition:
A system of arithmetic where numbers wrap around after reaching a certain value.

Operations:
- modulus
- addition
- subtraction
- multiplication

Relationships:
- used_in -> Algorithm

Time Complexity:
- None

Real Life Examples:
- Clock arithmetic
- Hash functions

Interview Questions:
- What is Modular Arithmetic? | A system of arithmetic where numbers wrap around after reaching a certain value.
- When should we use Modular Arithmetic? | Use Modular Arithmetic when its structure, operations, or performance match the problem requirement, especially in Mathematics topics.

CPP Code:
int result = a % b;

Python Code:

Java Code:

Images:
- Modular Arithmetic Diagram -> images/modular_arithmetic_diagram.png
- Modular Arithmetic Example -> images/modular_arithmetic_example.png

Step By Step:
- Identify the Modular Arithmetic use case
- Choose the required operation such as modulus
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 64
Topic: Algorithm
Category: Fundamental Concept
Difficulty: Easy

Definition:
A step-by-step procedure or set of rules to be followed in calculations or other problem-solving operations.

Operations:
- execute
- analyze

Relationships:
- uses -> Data Structure

Time Complexity:
- None

Real Life Examples:
- Cooking recipes
- Google Search

Interview Questions:
- What is Algorithm? | A step-by-step procedure or set of rules to be followed in calculations or other problem-solving operations.
- When should we use Algorithm? | Use Algorithm when its structure, operations, or performance match the problem requirement, especially in Fundamental Concept topics.

CPP Code:
def add(a, b): return a + b

Python Code:

Java Code:

Images:
- Algorithm Diagram -> images/algorithm_diagram.png
- Algorithm Example -> images/algorithm_example.png

Step By Step:
- Identify the Algorithm use case
- Choose the required operation such as execute
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 65
Topic: Vector
Category: Linear Structure
Difficulty: Easy

Definition:
A dynamic array that can resize itself automatically when an element is inserted or deleted.

Operations:
- push_back
- pop_back
- access
- insert

Relationships:
- child_of -> Array

Time Complexity:
- access: O(1)
- insertion: O(1) amortized
- deletion: O(n)

Real Life Examples:
- Dynamic list of items in a shopping cart

Interview Questions:
- What is Vector? | A dynamic array that can resize itself automatically when an element is inserted or deleted.
- When should we use Vector? | Use Vector when its structure, operations, or performance match the problem requirement, especially in Linear Structure topics.

CPP Code:
std::vector<int> v;
ArrayList<Integer> list = new ArrayList<>();

Python Code:

Java Code:

Images:
- Vector Diagram -> images/vector_diagram.png
- Vector Example -> images/vector_example.png

Step By Step:
- Identify the Vector use case
- Choose the required operation such as push_back
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 66
Topic: Matrix
Category: Linear Structure
Difficulty: Easy

Definition:
A collection of numbers arranged in a fixed number of rows and columns.

Operations:
- access
- addition
- multiplication
- transpose

Relationships:
- is_a -> Multidimensional Array

Time Complexity:
- access: O(1)

Real Life Examples:
- Image pixels
- Spreadsheet

Interview Questions:
- What is Matrix? | A collection of numbers arranged in a fixed number of rows and columns.
- When should we use Matrix? | Use Matrix when its structure, operations, or performance match the problem requirement, especially in Linear Structure topics.

CPP Code:
int matrix[3][3];

Python Code:

Java Code:

Images:
- Matrix Diagram -> images/matrix_diagram.png
- Matrix Example -> images/matrix_example.png

Step By Step:
- Identify the Matrix use case
- Choose the required operation such as access
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 67
Topic: Set
Category: Linear Structure
Difficulty: Easy

Definition:
A collection of distinct elements where no duplicate values are allowed.

Operations:
- insert
- delete
- search
- union
- intersection

Relationships:
- implements -> Hash Table

Time Complexity:
- search: O(1) or O(log n)
- insertion: O(1) or O(log n)

Real Life Examples:
- Unique IDs
- Distinct words in a book

Interview Questions:
- What is Set? | A collection of distinct elements where no duplicate values are allowed.
- When should we use Set? | Use Set when its structure, operations, or performance match the problem requirement, especially in Linear Structure topics.

CPP Code:
Set<Integer> s = new HashSet<>();
my_set = {1, 2, 3}

Python Code:

Java Code:

Images:
- Set Diagram -> images/set_diagram.png
- Set Example -> images/set_example.png

Step By Step:
- Identify the Set use case
- Choose the required operation such as insert
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 68
Topic: Map
Category: Linear Structure
Difficulty: Easy

Definition:
A collection of key-value pairs where each key is unique and maps to a specific value.

Operations:
- put
- get
- remove
- contains_key

Relationships:
- implements -> Hash Table

Time Complexity:
- search: O(1) or O(log n)
- insertion: O(1) or O(log n)

Real Life Examples:
- Dictionary
- Phone book

Interview Questions:
- What is Map? | A collection of key-value pairs where each key is unique and maps to a specific value.
- When should we use Map? | Use Map when its structure, operations, or performance match the problem requirement, especially in Linear Structure topics.

CPP Code:
Map<String, Int> m = new HashMap<>();
my_dict = {'key': 'value'}

Python Code:

Java Code:

Images:
- Map Diagram -> images/map_diagram.png
- Map Example -> images/map_example.png

Step By Step:
- Identify the Map use case
- Choose the required operation such as put
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 69
Topic: Polynomial
Category: Mathematics
Difficulty: Easy

Definition:
An expression consisting of variables and coefficients, involving only addition, subtraction, and multiplication.

Operations:
- addition
- multiplication
- evaluation
- differentiation

Relationships:
- related_to -> Linked List

Time Complexity:
- None

Real Life Examples:
- Physics formulas
- Economic modeling

Interview Questions:
- What is Polynomial? | An expression consisting of variables and coefficients, involving only addition, subtraction, and multiplication.
- When should we use Polynomial? | Use Polynomial when its structure, operations, or performance match the problem requirement, especially in Mathematics topics.

CPP Code:
3x^2 + 2x + 5

Python Code:

Java Code:

Images:
- Polynomial Diagram -> images/polynomial_diagram.png
- Polynomial Example -> images/polynomial_example.png

Step By Step:
- Identify the Polynomial use case
- Choose the required operation such as addition
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 70
Topic: Sorted Data
Category: Fundamental Concept
Difficulty: Easy

Definition:
Data that has been arranged in a systematic order, such as numerical or alphabetical.

Operations:
- binary search
- merge

Relationships:
- used_in -> Search Algorithm

Time Complexity:
- search: O(log n)

Real Life Examples:
- Alphabetized contact list
- Leaderboard

Interview Questions:
- What is Sorted Data? | Data that has been arranged in a systematic order, such as numerical or alphabetical.
- When should we use Sorted Data? | Use Sorted Data when its structure, operations, or performance match the problem requirement, especially in Fundamental Concept topics.

CPP Code:
[1, 2, 3, 4, 5]

Python Code:

Java Code:

Images:
- Sorted Data Diagram -> images/sorted_data_diagram.png
- Sorted Data Example -> images/sorted_data_example.png

Step By Step:
- Identify the Sorted Data use case
- Choose the required operation such as binary search
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 71
Topic: Search Algorithm
Category: Algorithm
Difficulty: Medium

Definition:
An algorithm designed to find a target element or determine whether it exists in a collection of data.

Operations:
- search
- compare
- traverse
- probe

Relationships:
- uses -> Data Structure
- uses -> Sorted Data
- is_a -> Algorithm

Time Complexity:
- best: O(1)
- average: O(log n)
- worst: O(n)

Real Life Examples:
- Finding a name in a phone book
- Searching for a word in a dictionary
- Looking up a record in a database

Interview Questions:
- What is Search Algorithm? | An algorithm designed to find a target element or determine whether it exists in a collection of data.
- When should we use Search Algorithm? | Use Search Algorithm when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
int binarySearch(vector<int>& arr, int target, int low, int high) { /* ... */ }

Python Code:

Java Code:

Images:
- Search Algorithm Diagram -> images/search_algorithm_diagram.png
- Search Algorithm Example -> images/search_algorithm_example.png

Step By Step:
- Identify the Search Algorithm use case
- Choose the required operation such as search
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 72
Topic: Complete Binary Tree
Category: Non-Linear Structure
Difficulty: Medium

Definition:
A binary tree in which every level is completely filled, except possibly the last level, which is filled from left to right.

Operations:
- insert
- delete
- traverse

Relationships:
- child_of -> Binary Tree
- related_to -> Heap

Time Complexity:
- height: O(log n)

Real Life Examples:
- Binary Heap storage

Interview Questions:
- What is Complete Binary Tree? | A binary tree in which every level is completely filled, except possibly the last level, which is filled from left to right.
- When should we use Complete Binary Tree? | Use Complete Binary Tree when its structure, operations, or performance match the problem requirement, especially in Non-Linear Structure topics.

CPP Code:

Python Code:

Java Code:

Images:
- Complete Binary Tree Diagram -> images/complete_binary_tree_diagram.png
- Complete Binary Tree Example -> images/complete_binary_tree_example.png

Step By Step:
- Identify the Complete Binary Tree use case
- Choose the required operation such as insert
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 73
Topic: Tree Traversal
Category: Algorithm
Difficulty: Medium

Definition:
The process of visiting all nodes in a tree in a specific order.

Operations:
- inorder
- preorder
- postorder
- level-order

Relationships:
- uses -> Tree

Time Complexity:
- traversal: O(n)

Real Life Examples:
- File system exploration

Interview Questions:
- What is Tree Traversal? | The process of visiting all nodes in a tree in a specific order.
- When should we use Tree Traversal? | Use Tree Traversal when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
void preorder(Node n) { ... }

Python Code:

Java Code:

Images:
- Tree Traversal Diagram -> images/tree_traversal_diagram.png
- Tree Traversal Example -> images/tree_traversal_example.png

Step By Step:
- Identify the Tree Traversal use case
- Choose the required operation such as inorder
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 74
Topic: BFS
Category: Algorithm
Difficulty: Medium

Definition:
Breadth-First Search: An algorithm for traversing or searching tree or graph data structures level by level.

Operations:
- traverse
- shortest path

Relationships:
- uses -> Queue
- uses -> Graph

Time Complexity:
- traversal: O(V + E)

Real Life Examples:
- GPS navigation
- Social networking (friend levels)

Interview Questions:
- What is BFS? | Breadth-First Search: An algorithm for traversing or searching tree or graph data structures level by level.
- When should we use BFS? | Use BFS when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
queue.enqueue(root);

Python Code:

Java Code:

Images:
- BFS Diagram -> images/bfs_diagram.png
- BFS Example -> images/bfs_example.png

Step By Step:
- Identify the BFS use case
- Choose the required operation such as traverse
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 75
Topic: DFS
Category: Algorithm
Difficulty: Medium

Definition:
Depth-First Search: An algorithm for traversing or searching tree or graph data structures by exploring as far as possible along each branch before backtracking.

Operations:
- traverse
- pathfinding

Relationships:
- uses -> Stack
- uses -> Recursion
- uses -> Graph

Time Complexity:
- traversal: O(V + E)

Real Life Examples:
- Solving puzzles (mazes)

Interview Questions:
- What is DFS? | Depth-First Search: An algorithm for traversing or searching tree or graph data structures by exploring as far as possible along each branch before backtracking.
- When should we use DFS? | Use DFS when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
dfs(vertex) { visited[vertex]=true; for... }

Python Code:

Java Code:

Images:
- DFS Diagram -> images/dfs_diagram.png
- DFS Example -> images/dfs_example.png

Step By Step:
- Identify the DFS use case
- Choose the required operation such as traverse
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 76
Topic: DAG
Category: Non-Linear Structure
Difficulty: Medium

Definition:
Directed Acyclic Graph: A directed graph with no directed cycles.

Operations:
- topological sort
- shortest path

Relationships:
- child_of -> Graph

Time Complexity:
- None

Real Life Examples:
- Task scheduling
- Git commit history

Interview Questions:
- What is DAG? | Directed Acyclic Graph: A directed graph with no directed cycles.
- When should we use DAG? | Use DAG when its structure, operations, or performance match the problem requirement, especially in Non-Linear Structure topics.

CPP Code:

Python Code:

Java Code:

Images:
- DAG Diagram -> images/dag_diagram.png
- DAG Example -> images/dag_example.png

Step By Step:
- Identify the DAG use case
- Choose the required operation such as topological sort
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 77
Topic: Weighted Undirected Graph
Category: Non-Linear Structure
Difficulty: Medium

Definition:
An undirected graph where each edge has an associated numerical value (weight).

Operations:
- find minimum spanning tree
- shortest path

Relationships:
- child_of -> Graph

Time Complexity:
- None

Real Life Examples:
- Road networks with distances

Interview Questions:
- What is Weighted Undirected Graph? | An undirected graph where each edge has an associated numerical value (weight).
- When should we use Weighted Undirected Graph? | Use Weighted Undirected Graph when its structure, operations, or performance match the problem requirement, especially in Non-Linear Structure topics.

CPP Code:

Python Code:

Java Code:

Images:
- Weighted Undirected Graph Diagram -> images/weighted_undirected_graph_diagram.png
- Weighted Undirected Graph Example -> images/weighted_undirected_graph_example.png

Step By Step:
- Identify the Weighted Undirected Graph use case
- Choose the required operation such as find minimum spanning tree
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 78
Topic: Shortest Path (unweighted graphs)
Category: Algorithm
Difficulty: Medium

Definition:
Finding the path between two vertices such that the number of edges is minimized.

Operations:
- find_path

Relationships:
- implements -> BFS

Time Complexity:
- search: O(V + E)

Real Life Examples:
- Six degrees of separation

Interview Questions:
- What is Shortest Path (unweighted graphs)? | Finding the path between two vertices such that the number of edges is minimized.
- When should we use Shortest Path (unweighted graphs)? | Use Shortest Path (unweighted graphs) when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:

Python Code:

Java Code:

Images:
- Shortest Path (unweighted graphs) Diagram -> images/shortest_path_unweighted_graphs_diagram.png
- Shortest Path (unweighted graphs) Example -> images/shortest_path_unweighted_graphs_example.png

Step By Step:
- Identify the Shortest Path (unweighted graphs) use case
- Choose the required operation such as find_path
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 79
Topic: Connected Components
Category: Algorithm
Difficulty: Medium

Definition:
Subgraphs in which any two vertices are connected to each other by paths.

Operations:
- count_components
- find_components

Relationships:
- uses -> BFS
- uses -> DFS

Time Complexity:
- find: O(V + E)

Real Life Examples:
- Social network clusters

Interview Questions:
- What is Connected Components? | Subgraphs in which any two vertices are connected to each other by paths.
- When should we use Connected Components? | Use Connected Components when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:

Python Code:

Java Code:

Images:
- Connected Components Diagram -> images/connected_components_diagram.png
- Connected Components Example -> images/connected_components_example.png

Step By Step:
- Identify the Connected Components use case
- Choose the required operation such as count_components
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 80
Topic: Spanning Tree
Category: Non-Linear Structure
Difficulty: Medium

Definition:
A subset of an undirected graph that connects all vertices together without any cycles.

Operations:
- construct

Relationships:
- related_to -> Graph

Time Complexity:
- None

Real Life Examples:
- Network design (cabling)

Interview Questions:
- What is Spanning Tree? | A subset of an undirected graph that connects all vertices together without any cycles.
- When should we use Spanning Tree? | Use Spanning Tree when its structure, operations, or performance match the problem requirement, especially in Non-Linear Structure topics.

CPP Code:

Python Code:

Java Code:

Images:
- Spanning Tree Diagram -> images/spanning_tree_diagram.png
- Spanning Tree Example -> images/spanning_tree_example.png

Step By Step:
- Identify the Spanning Tree use case
- Choose the required operation such as construct
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 81
Topic: Topological Sort
Category: Algorithm
Difficulty: Hard

Definition:
A linear ordering of vertices such that for every directed edge (u, v), vertex u comes before v.

Operations:
- sort

Relationships:
- uses -> DAG
- uses -> DFS

Time Complexity:
- sort: O(V + E)

Real Life Examples:
- University course prerequisites
- Build systems

Interview Questions:
- What is Topological Sort? | A linear ordering of vertices such that for every directed edge (u, v), vertex u comes before v.
- When should we use Topological Sort? | Use Topological Sort when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
Kahn's Algorithm

Python Code:

Java Code:

Images:
- Topological Sort Diagram -> images/topological_sort_diagram.png
- Topological Sort Example -> images/topological_sort_example.png

Step By Step:
- Identify the Topological Sort use case
- Choose the required operation such as sort
- Apply the operation while maintaining the data structure rules
- Analyze the time and space complexity

==================================================
Topic ID: 82
Topic: Dynamic Programming
Category: Algorithm
Difficulty: Hard

Definition:
An optimization technique that solves complex problems by breaking them into overlapping subproblems and storing previous results.

Operations:
- define states
- write recurrence
- initialize base cases
- fill DP table
- return answer

Relationships:
- uses -> Recursion
- related_to -> Memoization
- related_to -> Tabulation

Time Complexity:
- Main operations: Depends on implementation

Real Life Examples:
- Fibonacci optimization
- Knapsack problem
- Path counting

Interview Questions:
- What is Dynamic Programming? | An optimization technique that solves complex problems by breaking them into overlapping subproblems and storing previous results.
- When should we use Dynamic Programming? | Use Dynamic Programming when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
vector<int> dp(n + 1);
dp[0] = 0;
dp[1] = 1;
for(int i = 2; i <= n; i++) {
    dp[i] = dp[i - 1] + dp[i - 2];
}

Python Code:

Java Code:

Images:
- Dynamic Programming Diagram -> images/dynamic_programming_diagram.png
- Dynamic Programming Example -> images/dynamic_programming_example.png

Step By Step:
- Define the subproblem
- Find the recurrence
- Set base cases
- Compute results using memoization or tabulation
- Return the final state

==================================================
Topic ID: 83
Topic: Greedy Algorithm
Category: Algorithm
Difficulty: Medium

Definition:
An algorithmic strategy that makes the locally best choice at each step hoping to reach a globally optimal solution.

Operations:
- choose best local option
- update state
- repeat until complete

Relationships:
- related_to -> Algorithm
- used_in -> Prim Algorithm
- used_in -> Kruskal Algorithm

Time Complexity:
- Main operations: Depends on implementation

Real Life Examples:
- Coin change with canonical coin systems
- Activity selection
- Minimum spanning tree algorithms

Interview Questions:
- What is Greedy Algorithm? | An algorithmic strategy that makes the locally best choice at each step hoping to reach a globally optimal solution.
- When should we use Greedy Algorithm? | Use Greedy Algorithm when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
sort(items.begin(), items.end());
for(auto item : items) {
    if(canChoose(item)) choose(item);
}

Python Code:

Java Code:

Images:
- Greedy Algorithm Diagram -> images/greedy_algorithm_diagram.png
- Greedy Algorithm Example -> images/greedy_algorithm_example.png

Step By Step:
- Sort or prioritize choices
- Select the best available option
- Check if the choice is valid
- Update the solution
- Repeat until complete

==================================================
Topic ID: 84
Topic: Backtracking
Category: Algorithm
Difficulty: Medium

Definition:
A recursive problem-solving technique that builds candidates step by step and abandons a candidate when it cannot lead to a valid solution.

Operations:
- choose
- explore
- unchoose
- validate

Relationships:
- uses -> Recursion
- uses -> DFS
- related_to -> Tree Traversal

Time Complexity:
- Main operations: Depends on implementation

Real Life Examples:
- N-Queens problem
- Sudoku solver
- Generating permutations

Interview Questions:
- What is Backtracking? | A recursive problem-solving technique that builds candidates step by step and abandons a candidate when it cannot lead to a valid solution.
- When should we use Backtracking? | Use Backtracking when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
void backtrack() {
    if(done()) return;
    for(auto choice : choices) {
        choose(choice);
        backtrack();
        undo(choice);
    }
}

Python Code:

Java Code:

Images:
- Backtracking Diagram -> images/backtracking_diagram.png
- Backtracking Example -> images/backtracking_example.png

Step By Step:
- Choose a candidate
- Check whether it is valid
- Recursively explore the next choice
- Backtrack by undoing the choice
- Continue until all solutions are found

==================================================
Topic ID: 85
Topic: Sliding Window
Category: Algorithm
Difficulty: Medium

Definition:
A technique used to process contiguous subarrays or substrings efficiently by maintaining a moving window over the data.

Operations:
- expand window
- shrink window
- update answer

Relationships:
- uses -> Array
- related_to -> Queue

Time Complexity:
- Main operations: Depends on implementation

Real Life Examples:
- Maximum sum subarray of size k
- Longest substring without repeating characters

Interview Questions:
- What is Sliding Window? | A technique used to process contiguous subarrays or substrings efficiently by maintaining a moving window over the data.
- When should we use Sliding Window? | Use Sliding Window when its structure, operations, or performance match the problem requirement, especially in Algorithm topics.

CPP Code:
int left = 0;
for(int right = 0; right < n; right++) {
    // add arr[right]
    while(windowInvalid()) {
        // remove arr[left]
        left++;
    }
}

Python Code:

Java Code:

Images:
- Sliding Window Diagram -> images/sliding_window_diagram.png
- Sliding Window Example -> images/sliding_window_example.png

Step By Step:
- Initialize left and right pointers
- Expand the right pointer
- Update window information
- Shrink the left pointer when needed
- Store the best answer

==================================================
Topic ID: 86
Topic: Union Find
Category: Specialized Structure
Difficulty: Hard

Definition:
A data structure that keeps track of disjoint sets and supports efficient union and find operations.

Operations:
- find
- union
- path compression
- union by rank

Relationships:
- used_in -> Kruskal Algorithm
- related_to -> Graph

Time Complexity:
- Main operations: Depends on implementation

Real Life Examples:
- Cycle detection in graphs
- Kruskal algorithm
- Network connectivity

Interview Questions:
- What is Union Find? | A data structure that keeps track of disjoint sets and supports efficient union and find operations.
- When should we use Union Find? | Use Union Find when its structure, operations, or performance match the problem requirement, especially in Specialized Structure topics.

CPP Code:
int find(int x) {
    if(parent[x] == x) return x;
    return parent[x] = find(parent[x]);
}

Python Code:

Java Code:

Images:
- Union Find Diagram -> images/union_find_diagram.png
- Union Find Example -> images/union_find_example.png

Step By Step:
- Initialize each element as its own parent
- Find the representative of each set
- Union two sets if their roots differ
- Apply path compression to speed up future finds

==================================================
Topic ID: 87
Topic: Trie
Category: Specialized Structure
Difficulty: Medium

Definition:
A tree-like data structure used to store strings where each path represents a prefix.

Operations:
- insert word
- search word
- startsWith prefix

Relationships:
- is_a -> Tree
- used_in -> String Matching

Time Complexity:
- Main operations: Depends on implementation

Real Life Examples:
- Autocomplete
- Spell checker
- Dictionary search

Interview Questions:
- What is Trie? | A tree-like data structure used to store strings where each path represents a prefix.
- When should we use Trie? | Use Trie when its structure, operations, or performance match the problem requirement, especially in Specialized Structure topics.

CPP Code:
struct TrieNode {
    TrieNode* child[26];
    bool isEnd;
};

Python Code:

Java Code:

Images:
- Trie Diagram -> images/trie_diagram.png
- Trie Example -> images/trie_example.png

Step By Step:
- Start from the root
- For each character, move to or create a child node
- Mark the end of a word
- Search by following character links
==================================================
Topic: Memoization
Category: Algorithmic Technique
Difficulty: Medium

Definition:
Memoization stores previously computed results so repeated calculations can be avoided.

Operations:
- Store result
- Check cache
- Reuse result

Relationships:
- used_in -> Dynamic Programming
- related_to -> Recursion

Time Complexity:
- lookup: O(1)

Real Life Examples:
- Caching website data
- Saving repeated calculations

Interview Questions:
- What is memoization? | Memoization is an optimization technique that stores computed results for reuse.

CPP Code:
unordered_map<int, int> memo;

Python Code:
memo = {}

Java Code:
HashMap<Integer, Integer> memo = new HashMap<>();

Images:
- Memoization Cache -> images/memoization_cache.png

Step By Step:
- Check if answer exists in cache
- If yes, return cached result
- If no, compute answer
- Store answer in cache
- Return result
==================================================
Topic: Tabulation
Category: Algorithmic Technique
Difficulty: Medium

Definition:
Tabulation is a bottom-up dynamic programming method that solves smaller subproblems first and stores their results in a table.

Operations:
- Create table
- Fill base cases
- Build solution bottom-up

Relationships:
- used_in -> Dynamic Programming
- related_to -> Memoization

Time Complexity:
- build table: O(n)

Real Life Examples:
- Building a score table
- Filling spreadsheet calculations

Interview Questions:
- What is tabulation? | Tabulation solves subproblems iteratively from the smallest case to the final answer.

CPP Code:
vector<int> dp(n + 1, 0);

Python Code:
dp = [0] * (n + 1)

Java Code:
int[] dp = new int[n + 1];

Images:
- Tabulation Table -> images/tabulation_table.png

Step By Step:
- Create a table
- Set base cases
- Fill table from smaller problems
- Use final cell as answer
==================================================
Topic: String Matching
Category: Algorithm
Difficulty: Medium

Definition:
String matching is the process of finding whether a pattern appears inside a text.

Operations:
- compare characters
- shift pattern
- find match

Relationships:
- used_in -> Trie
- related_to -> Algorithm

Time Complexity:
- naive search: O(n*m)

Real Life Examples:
- Search bar autocomplete
- Finding words in documents

Interview Questions:
- What is string matching? | String matching finds occurrences of a pattern inside a larger text.

CPP Code:
text.find(pattern);

Python Code:
pattern in text

Java Code:
text.contains(pattern);

Images:
- String Matching Example -> images/string_matching.png

Step By Step:
- Choose a pattern
- Compare pattern with text
- Move to next position if not matched
- Return match position if found